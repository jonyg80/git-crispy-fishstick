name: platform/external/private-join-and-compute Clone and Push

on:
  schedule:
    - cron: '24 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/private-join-and-compute platform/external/private-join-and-compute

    - name: Push
      run: |
        cd platform/external/private-join-and-compute
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/private-join-and-compute.git