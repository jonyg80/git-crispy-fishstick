name: kernel/google-modules/aoc Clone and Push

on:
  schedule:
    - cron: '18 9 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/kernel/google-modules/aoc kernel/google-modules/aoc

    - name: Push
      run: |
        cd kernel/google-modules/aoc
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/kernel/google-modules/aoc.git