name: platform/frameworks/libs/binary_translation Clone and Push

on:
  schedule:
    - cron: '32 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/libs/binary_translation platform/frameworks/libs/binary_translation

    - name: Push
      run: |
        cd platform/frameworks/libs/binary_translation
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/libs/binary_translation.git