name: platform/external/rust/crates/userfaultfd Clone and Push

on:
  schedule:
    - cron: '31 9 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/userfaultfd platform/external/rust/crates/userfaultfd

    - name: Push
      run: |
        cd platform/external/rust/crates/userfaultfd
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/userfaultfd.git