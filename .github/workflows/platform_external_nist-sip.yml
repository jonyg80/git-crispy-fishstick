name: platform/external/nist-sip Clone and Push

on:
  schedule:
    - cron: '25 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/nist-sip platform/external/nist-sip

    - name: Push
      run: |
        cd platform/external/nist-sip
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/nist-sip.git