name: platform/external/xmp_toolkit Clone and Push

on:
  schedule:
    - cron: '12 4 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/xmp_toolkit platform/external/xmp_toolkit

    - name: Push
      run: |
        cd platform/external/xmp_toolkit
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/xmp_toolkit.git