name: platform/packages/screensavers/PhotoTable Clone and Push

on:
  schedule:
    - cron: '18 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/screensavers/PhotoTable platform/packages/screensavers/PhotoTable

    - name: Push
      run: |
        cd platform/packages/screensavers/PhotoTable
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/screensavers/PhotoTable.git