name: kernel/goldfish-modules Clone and Push

on:
  schedule:
    - cron: '12 9 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/kernel/goldfish-modules kernel/goldfish-modules

    - name: Push
      run: |
        cd kernel/goldfish-modules
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/kernel/goldfish-modules.git