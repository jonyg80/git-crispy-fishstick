name: platform/external/protobuf-javalite Clone and Push

on:
  schedule:
    - cron: '27 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/protobuf-javalite platform/external/protobuf-javalite

    - name: Push
      run: |
        cd platform/external/protobuf-javalite
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/protobuf-javalite.git