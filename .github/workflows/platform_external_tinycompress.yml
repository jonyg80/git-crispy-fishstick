name: platform/external/tinycompress Clone and Push

on:
  schedule:
    - cron: '12 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/tinycompress platform/external/tinycompress

    - name: Push
      run: |
        cd platform/external/tinycompress
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/tinycompress.git