name: platform/prebuilts/vndk/v27 Clone and Push

on:
  schedule:
    - cron: '48 15 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/vndk/v27 platform/prebuilts/vndk/v27

    - name: Push
      run: |
        cd platform/prebuilts/vndk/v27
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/vndk/v27.git