name: platform/hardware/nxp/secure_element Clone and Push

on:
  schedule:
    - cron: '49 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/nxp/secure_element platform/hardware/nxp/secure_element

    - name: Push
      run: |
        cd platform/hardware/nxp/secure_element
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/nxp/secure_element.git