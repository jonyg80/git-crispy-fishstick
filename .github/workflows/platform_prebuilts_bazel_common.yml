name: platform/prebuilts/bazel/common Clone and Push

on:
  schedule:
    - cron: '5 20 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/bazel/common platform/prebuilts/bazel/common

    - name: Push
      run: |
        cd platform/prebuilts/bazel/common
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/bazel/common.git