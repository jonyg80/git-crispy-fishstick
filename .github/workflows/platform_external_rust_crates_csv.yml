name: platform/external/rust/crates/csv Clone and Push

on:
  schedule:
    - cron: '21 5 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/csv platform/external/rust/crates/csv

    - name: Push
      run: |
        cd platform/external/rust/crates/csv
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/csv.git