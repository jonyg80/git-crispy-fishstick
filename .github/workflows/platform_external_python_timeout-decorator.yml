name: platform/external/python/timeout-decorator Clone and Push

on:
  schedule:
    - cron: '3 3 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/timeout-decorator platform/external/python/timeout-decorator

    - name: Push
      run: |
        cd platform/external/python/timeout-decorator
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/python/timeout-decorator.git