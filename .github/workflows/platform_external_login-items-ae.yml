name: platform/external/login-items-ae Clone and Push

on:
  schedule:
    - cron: '35 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/login-items-ae platform/external/login-items-ae

    - name: Push
      run: |
        cd platform/external/login-items-ae
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/login-items-ae.git