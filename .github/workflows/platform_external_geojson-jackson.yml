name: platform/external/geojson-jackson Clone and Push

on:
  schedule:
    - cron: '54 4 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/geojson-jackson platform/external/geojson-jackson

    - name: Push
      run: |
        cd platform/external/geojson-jackson
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/geojson-jackson.git