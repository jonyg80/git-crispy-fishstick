name: platform/hardware/broadcom/libbt Clone and Push

on:
  schedule:
    - cron: '15 8 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/broadcom/libbt platform/hardware/broadcom/libbt

    - name: Push
      run: |
        cd platform/hardware/broadcom/libbt
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/broadcom/libbt.git