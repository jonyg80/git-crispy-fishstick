name: platform/external/linux-tools-perf Clone and Push

on:
  schedule:
    - cron: '26 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/linux-tools-perf platform/external/linux-tools-perf

    - name: Push
      run: |
        cd platform/external/linux-tools-perf
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/linux-tools-perf.git