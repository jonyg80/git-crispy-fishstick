name: platform/hardware/nxp/keymint Clone and Push

on:
  schedule:
    - cron: '6 12 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/nxp/keymint platform/hardware/nxp/keymint

    - name: Push
      run: |
        cd platform/hardware/nxp/keymint
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/nxp/keymint.git