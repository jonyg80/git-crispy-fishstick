name: platform/prebuilts/vndk/v34 Clone and Push

on:
  schedule:
    - cron: '8 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/vndk/v34 platform/prebuilts/vndk/v34

    - name: Push
      run: |
        cd platform/prebuilts/vndk/v34
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/vndk/v34.git