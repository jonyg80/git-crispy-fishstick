name: platform/system/nativepower Clone and Push

on:
  schedule:
    - cron: '12 19 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/nativepower platform/system/nativepower

    - name: Push
      run: |
        cd platform/system/nativepower
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/system/nativepower.git