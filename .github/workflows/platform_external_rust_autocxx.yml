name: platform/external/rust/autocxx Clone and Push

on:
  schedule:
    - cron: '7 4 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/autocxx platform/external/rust/autocxx

    - name: Push
      run: |
        cd platform/external/rust/autocxx
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/autocxx.git