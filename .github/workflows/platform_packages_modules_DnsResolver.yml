name: platform/packages/modules/DnsResolver Clone and Push

on:
  schedule:
    - cron: '0 23 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/DnsResolver platform/packages/modules/DnsResolver

    - name: Push
      run: |
        cd platform/packages/modules/DnsResolver
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/modules/DnsResolver.git