name: platform/system/libsysprop Clone and Push

on:
  schedule:
    - cron: '54 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/libsysprop platform/system/libsysprop

    - name: Push
      run: |
        cd platform/system/libsysprop
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/libsysprop.git