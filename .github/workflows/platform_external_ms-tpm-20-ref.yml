name: platform/external/ms-tpm-20-ref Clone and Push

on:
  schedule:
    - cron: '9 19 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/ms-tpm-20-ref platform/external/ms-tpm-20-ref

    - name: Push
      run: |
        cd platform/external/ms-tpm-20-ref
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/ms-tpm-20-ref.git