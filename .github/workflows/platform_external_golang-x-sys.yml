name: platform/external/golang-x-sys Clone and Push

on:
  schedule:
    - cron: '45 5 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/golang-x-sys platform/external/golang-x-sys

    - name: Push
      run: |
        cd platform/external/golang-x-sys
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/golang-x-sys.git