name: platform/packages/apps/TvSystemUI Clone and Push

on:
  schedule:
    - cron: '36 21 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/TvSystemUI platform/packages/apps/TvSystemUI

    - name: Push
      run: |
        cd platform/packages/apps/TvSystemUI
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/TvSystemUI.git