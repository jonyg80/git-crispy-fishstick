name: platform/hardware/qcom/sdm845/media Clone and Push

on:
  schedule:
    - cron: '51 13 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/qcom/sdm845/media platform/hardware/qcom/sdm845/media

    - name: Push
      run: |
        cd platform/hardware/qcom/sdm845/media
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/qcom/sdm845/media.git