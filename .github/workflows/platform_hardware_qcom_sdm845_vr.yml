name: platform/hardware/qcom/sdm845/vr Clone and Push

on:
  schedule:
    - cron: '24 15 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/qcom/sdm845/vr platform/hardware/qcom/sdm845/vr

    - name: Push
      run: |
        cd platform/hardware/qcom/sdm845/vr
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/qcom/sdm845/vr.git