name: platform/external/arm-neon-tests Clone and Push

on:
  schedule:
    - cron: '42 18 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/arm-neon-tests platform/external/arm-neon-tests

    - name: Push
      run: |
        cd platform/external/arm-neon-tests
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/arm-neon-tests.git