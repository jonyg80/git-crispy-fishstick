name: platform/prebuilts/module_sdk/OnDevicePersonalization Clone and Push

on:
  schedule:
    - cron: '54 13 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/module_sdk/OnDevicePersonalization platform/prebuilts/module_sdk/OnDevicePersonalization

    - name: Push
      run: |
        cd platform/prebuilts/module_sdk/OnDevicePersonalization
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/module_sdk/OnDevicePersonalization.git