name: platform/external/golang-groupcache Clone and Push

on:
  schedule:
    - cron: '30 5 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/golang-groupcache platform/external/golang-groupcache

    - name: Push
      run: |
        cd platform/external/golang-groupcache
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/golang-groupcache.git