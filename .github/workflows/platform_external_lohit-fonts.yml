name: platform/external/lohit-fonts Clone and Push

on:
  schedule:
    - cron: '36 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/lohit-fonts platform/external/lohit-fonts

    - name: Push
      run: |
        cd platform/external/lohit-fonts
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/lohit-fonts.git