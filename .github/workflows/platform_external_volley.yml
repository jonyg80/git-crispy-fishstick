name: platform/external/volley Clone and Push

on:
  schedule:
    - cron: '44 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/volley platform/external/volley

    - name: Push
      run: |
        cd platform/external/volley
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/volley.git