name: platform/external/libxkbcommon Clone and Push

on:
  schedule:
    - cron: '45 16 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/libxkbcommon platform/external/libxkbcommon

    - name: Push
      run: |
        cd platform/external/libxkbcommon
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/libxkbcommon.git