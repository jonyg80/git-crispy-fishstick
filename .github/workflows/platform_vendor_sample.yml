name: platform/vendor/sample Clone and Push

on:
  schedule:
    - cron: '17 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/vendor/sample platform/vendor/sample

    - name: Push
      run: |
        cd platform/vendor/sample
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/vendor/sample.git