name: platform/packages/apps/Car/libs Clone and Push

on:
  schedule:
    - cron: '0 18 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/Car/libs platform/packages/apps/Car/libs

    - name: Push
      run: |
        cd platform/packages/apps/Car/libs
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/Car/libs.git