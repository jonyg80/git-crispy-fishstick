name: platform/packages/modules/Telephony Clone and Push

on:
  schedule:
    - cron: '49 18 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/Telephony platform/packages/modules/Telephony

    - name: Push
      run: |
        cd platform/packages/modules/Telephony
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/modules/Telephony.git