name: platform/prebuilts/fullsdk/build-tools/22-linux Clone and Push

on:
  schedule:
    - cron: '54 7 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/fullsdk/build-tools/22-linux platform/prebuilts/fullsdk/build-tools/22-linux

    - name: Push
      run: |
        cd platform/prebuilts/fullsdk/build-tools/22-linux
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/fullsdk/build-tools/22-linux.git