name: platform/packages/apps/SettingsIntelligence Clone and Push

on:
  schedule:
    - cron: '39 20 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/SettingsIntelligence platform/packages/apps/SettingsIntelligence

    - name: Push
      run: |
        cd platform/packages/apps/SettingsIntelligence
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/SettingsIntelligence.git