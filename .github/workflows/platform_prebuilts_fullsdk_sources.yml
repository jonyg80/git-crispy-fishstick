name: platform/prebuilts/fullsdk/sources Clone and Push

on:
  schedule:
    - cron: '36 8 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/fullsdk/sources platform/prebuilts/fullsdk/sources

    - name: Push
      run: |
        cd platform/prebuilts/fullsdk/sources
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/fullsdk/sources.git