name: platform/external/subsampling-scale-image-view Clone and Push

on:
  schedule:
    - cron: '53 10 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/subsampling-scale-image-view platform/external/subsampling-scale-image-view

    - name: Push
      run: |
        cd platform/external/subsampling-scale-image-view
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/subsampling-scale-image-view.git