name: platform/packages/modules/RemoteKeyProvisioning Clone and Push

on:
  schedule:
    - cron: '51 23 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/RemoteKeyProvisioning platform/packages/modules/RemoteKeyProvisioning

    - name: Push
      run: |
        cd platform/packages/modules/RemoteKeyProvisioning
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/modules/RemoteKeyProvisioning.git