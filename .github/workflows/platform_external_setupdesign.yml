name: platform/external/setupdesign Clone and Push

on:
  schedule:
    - cron: '21 10 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/setupdesign platform/external/setupdesign

    - name: Push
      run: |
        cd platform/external/setupdesign
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/setupdesign.git