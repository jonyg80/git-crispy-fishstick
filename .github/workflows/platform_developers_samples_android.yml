name: platform/developers/samples/android Clone and Push

on:
  schedule:
    - cron: '27 15 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/developers/samples/android platform/developers/samples/android

    - name: Push
      run: |
        cd platform/developers/samples/android
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/developers/samples/android.git