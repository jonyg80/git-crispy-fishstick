name: platform/system/memory/libion Clone and Push

on:
  schedule:
    - cron: '51 18 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/memory/libion platform/system/memory/libion

    - name: Push
      run: |
        cd platform/system/memory/libion
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/system/memory/libion.git