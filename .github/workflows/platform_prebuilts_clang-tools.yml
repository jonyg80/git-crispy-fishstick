name: platform/prebuilts/clang-tools Clone and Push

on:
  schedule:
    - cron: '39 4 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/clang-tools platform/prebuilts/clang-tools

    - name: Push
      run: |
        cd platform/prebuilts/clang-tools
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/clang-tools.git