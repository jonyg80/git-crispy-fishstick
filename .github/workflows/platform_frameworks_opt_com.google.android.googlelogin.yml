name: platform/frameworks/opt/com.google.android.googlelogin Clone and Push

on:
  schedule:
    - cron: '54 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/opt/com.google.android.googlelogin platform/frameworks/opt/com.google.android.googlelogin

    - name: Push
      run: |
        cd platform/frameworks/opt/com.google.android.googlelogin
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/opt/com.google.android.googlelogin.git