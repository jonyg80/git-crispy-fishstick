name: platform/external/rust/crates/gimli Clone and Push

on:
  schedule:
    - cron: '48 9 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/gimli platform/external/rust/crates/gimli

    - name: Push
      run: |
        cd platform/external/rust/crates/gimli
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/rust/crates/gimli.git