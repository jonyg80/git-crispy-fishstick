name: platform/external/libunwind Clone and Push

on:
  schedule:
    - cron: '3 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/libunwind platform/external/libunwind

    - name: Push
      run: |
        cd platform/external/libunwind
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/libunwind.git