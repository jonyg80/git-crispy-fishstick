name: platform/system/teeui Clone and Push

on:
  schedule:
    - cron: '17 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/teeui platform/system/teeui

    - name: Push
      run: |
        cd platform/system/teeui
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/teeui.git