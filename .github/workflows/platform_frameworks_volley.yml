name: platform/frameworks/volley Clone and Push

on:
  schedule:
    - cron: '25 13 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/volley platform/frameworks/volley

    - name: Push
      run: |
        cd platform/frameworks/volley
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/volley.git