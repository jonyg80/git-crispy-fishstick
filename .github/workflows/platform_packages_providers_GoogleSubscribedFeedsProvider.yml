name: platform/packages/providers/GoogleSubscribedFeedsProvider Clone and Push

on:
  schedule:
    - cron: '8 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/providers/GoogleSubscribedFeedsProvider platform/packages/providers/GoogleSubscribedFeedsProvider

    - name: Push
      run: |
        cd platform/packages/providers/GoogleSubscribedFeedsProvider
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/providers/GoogleSubscribedFeedsProvider.git