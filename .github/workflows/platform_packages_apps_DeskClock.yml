name: platform/packages/apps/DeskClock Clone and Push

on:
  schedule:
    - cron: '24 18 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/DeskClock platform/packages/apps/DeskClock

    - name: Push
      run: |
        cd platform/packages/apps/DeskClock
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/DeskClock.git