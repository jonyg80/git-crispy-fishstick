name: platform/hardware/google/easel Clone and Push

on:
  schedule:
    - cron: '12 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/google/easel platform/hardware/google/easel

    - name: Push
      run: |
        cd platform/hardware/google/easel
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/google/easel.git