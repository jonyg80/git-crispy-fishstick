name: platform/external/lottie Clone and Push

on:
  schedule:
    - cron: '37 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/lottie platform/external/lottie

    - name: Push
      run: |
        cd platform/external/lottie
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/lottie.git