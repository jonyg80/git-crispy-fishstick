name: platform/packages/providers/GoogleContactsProvider Clone and Push

on:
  schedule:
    - cron: '6 1 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/providers/GoogleContactsProvider platform/packages/providers/GoogleContactsProvider

    - name: Push
      run: |
        cd platform/packages/providers/GoogleContactsProvider
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/providers/GoogleContactsProvider.git