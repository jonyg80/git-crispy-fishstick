name: platform/prebuilts/vndk/v29 Clone and Push

on:
  schedule:
    - cron: '3 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/vndk/v29 platform/prebuilts/vndk/v29

    - name: Push
      run: |
        cd platform/prebuilts/vndk/v29
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/vndk/v29.git