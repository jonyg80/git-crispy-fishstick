name: platform/external/rust/crates/addr2line Clone and Push

on:
  schedule:
    - cron: '10 4 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/addr2line platform/external/rust/crates/addr2line

    - name: Push
      run: |
        cd platform/external/rust/crates/addr2line
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/addr2line.git