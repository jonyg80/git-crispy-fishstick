name: platform/external/ow2-asm Clone and Push

on:
  schedule:
    - cron: '51 21 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/ow2-asm platform/external/ow2-asm

    - name: Push
      run: |
        cd platform/external/ow2-asm
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/ow2-asm.git