name: platform/packages/modules/Scheduling Clone and Push

on:
  schedule:
    - cron: '45 18 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/Scheduling platform/packages/modules/Scheduling

    - name: Push
      run: |
        cd platform/packages/modules/Scheduling
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/modules/Scheduling.git