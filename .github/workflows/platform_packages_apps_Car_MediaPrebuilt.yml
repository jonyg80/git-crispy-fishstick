name: platform/packages/apps/Car/MediaPrebuilt Clone and Push

on:
  schedule:
    - cron: '28 16 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/Car/MediaPrebuilt platform/packages/apps/Car/MediaPrebuilt

    - name: Push
      run: |
        cd platform/packages/apps/Car/MediaPrebuilt
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/Car/MediaPrebuilt.git