name: platform/system/iot/attestation Clone and Push

on:
  schedule:
    - cron: '40 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/iot/attestation platform/system/iot/attestation

    - name: Push
      run: |
        cd platform/system/iot/attestation
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/iot/attestation.git