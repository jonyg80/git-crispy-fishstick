name: platform/external/trace-cmd Clone and Push

on:
  schedule:
    - cron: '21 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/trace-cmd platform/external/trace-cmd

    - name: Push
      run: |
        cd platform/external/trace-cmd
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/trace-cmd.git