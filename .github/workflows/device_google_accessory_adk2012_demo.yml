name: device/google/accessory/adk2012_demo Clone and Push

on:
  schedule:
    - cron: '0 2 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/device/google/accessory/adk2012_demo device/google/accessory/adk2012_demo

    - name: Push
      run: |
        cd device/google/accessory/adk2012_demo
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/device/google/accessory/adk2012_demo.git