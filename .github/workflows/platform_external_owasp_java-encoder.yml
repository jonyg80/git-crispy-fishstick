name: platform/external/owasp/java-encoder Clone and Push

on:
  schedule:
    - cron: '54 21 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/owasp/java-encoder platform/external/owasp/java-encoder

    - name: Push
      run: |
        cd platform/external/owasp/java-encoder
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/owasp/java-encoder.git