name: platform/external/openmp_llvm Clone and Push

on:
  schedule:
    - cron: '15 21 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/openmp_llvm platform/external/openmp_llvm

    - name: Push
      run: |
        cd platform/external/openmp_llvm
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/openmp_llvm.git