name: platform/packages/apps/StorageManager Clone and Push

on:
  schedule:
    - cron: '44 17 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/StorageManager platform/packages/apps/StorageManager

    - name: Push
      run: |
        cd platform/packages/apps/StorageManager
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/StorageManager.git