name: platform/hardware/bsp/kernel/rockchip/rk-v4.4 Clone and Push

on:
  schedule:
    - cron: '1 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/bsp/kernel/rockchip/rk-v4.4 platform/hardware/bsp/kernel/rockchip/rk-v4.4

    - name: Push
      run: |
        cd platform/hardware/bsp/kernel/rockchip/rk-v4.4
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/bsp/kernel/rockchip/rk-v4.4.git