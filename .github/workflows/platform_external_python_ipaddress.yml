name: platform/external/python/ipaddress Clone and Push

on:
  schedule:
    - cron: '7 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/ipaddress platform/external/python/ipaddress

    - name: Push
      run: |
        cd platform/external/python/ipaddress
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/python/ipaddress.git