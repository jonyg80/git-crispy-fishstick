name: platform/external/python/scipy Clone and Push

on:
  schedule:
    - cron: '43 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/scipy platform/external/python/scipy

    - name: Push
      run: |
        cd platform/external/python/scipy
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/python/scipy.git