name: platform/external/seccomp-tests Clone and Push

on:
  schedule:
    - cron: '33 22 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/seccomp-tests platform/external/seccomp-tests

    - name: Push
      run: |
        cd platform/external/seccomp-tests
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/seccomp-tests.git