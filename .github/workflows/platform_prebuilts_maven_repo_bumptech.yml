name: platform/prebuilts/maven_repo/bumptech Clone and Push

on:
  schedule:
    - cron: '9 23 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/maven_repo/bumptech platform/prebuilts/maven_repo/bumptech

    - name: Push
      run: |
        cd platform/prebuilts/maven_repo/bumptech
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/maven_repo/bumptech.git