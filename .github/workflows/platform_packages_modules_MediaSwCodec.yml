name: platform/packages/modules/MediaSwCodec Clone and Push

on:
  schedule:
    - cron: '34 18 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/MediaSwCodec platform/packages/modules/MediaSwCodec

    - name: Push
      run: |
        cd platform/packages/modules/MediaSwCodec
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/modules/MediaSwCodec.git