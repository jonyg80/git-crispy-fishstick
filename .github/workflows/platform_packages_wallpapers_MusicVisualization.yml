name: platform/packages/wallpapers/MusicVisualization Clone and Push

on:
  schedule:
    - cron: '39 2 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/wallpapers/MusicVisualization platform/packages/wallpapers/MusicVisualization

    - name: Push
      run: |
        cd platform/packages/wallpapers/MusicVisualization
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/wallpapers/MusicVisualization.git