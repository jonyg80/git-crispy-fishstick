name: platform/hardware/qcom/sm7150/display Clone and Push

on:
  schedule:
    - cron: '26 15 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/qcom/sm7150/display platform/hardware/qcom/sm7150/display

    - name: Push
      run: |
        cd platform/hardware/qcom/sm7150/display
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/qcom/sm7150/display.git