name: platform/external/e2fsprogs Clone and Push

on:
  schedule:
    - cron: '15 2 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/e2fsprogs platform/external/e2fsprogs

    - name: Push
      run: |
        cd platform/external/e2fsprogs
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/e2fsprogs.git