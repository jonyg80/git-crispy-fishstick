name: platform/hardware/qcom/camera Clone and Push

on:
  schedule:
    - cron: '55 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/qcom/camera platform/hardware/qcom/camera

    - name: Push
      run: |
        cd platform/hardware/qcom/camera
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/qcom/camera.git