name: platform/packages/apps/ImsServiceEntitlement Clone and Push

on:
  schedule:
    - cron: '10 17 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/ImsServiceEntitlement platform/packages/apps/ImsServiceEntitlement

    - name: Push
      run: |
        cd platform/packages/apps/ImsServiceEntitlement
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/ImsServiceEntitlement.git