name: platform/prebuilts/androidx/external Clone and Push

on:
  schedule:
    - cron: '58 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/androidx/external platform/prebuilts/androidx/external

    - name: Push
      run: |
        cd platform/prebuilts/androidx/external
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/androidx/external.git