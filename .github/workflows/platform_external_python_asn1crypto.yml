name: platform/external/python/asn1crypto Clone and Push

on:
  schedule:
    - cron: '38 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/asn1crypto platform/external/python/asn1crypto

    - name: Push
      run: |
        cd platform/external/python/asn1crypto
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/python/asn1crypto.git