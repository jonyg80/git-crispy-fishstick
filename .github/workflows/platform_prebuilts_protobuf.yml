name: platform/prebuilts/protobuf Clone and Push

on:
  schedule:
    - cron: '38 23 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/protobuf platform/prebuilts/protobuf

    - name: Push
      run: |
        cd platform/prebuilts/protobuf
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/protobuf.git