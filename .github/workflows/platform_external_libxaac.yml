name: platform/external/libxaac Clone and Push

on:
  schedule:
    - cron: '18 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/libxaac platform/external/libxaac

    - name: Push
      run: |
        cd platform/external/libxaac
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/libxaac.git