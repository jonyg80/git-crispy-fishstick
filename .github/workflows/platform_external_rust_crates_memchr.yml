name: platform/external/rust/crates/memchr Clone and Push

on:
  schedule:
    - cron: '51 6 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/memchr platform/external/rust/crates/memchr

    - name: Push
      run: |
        cd platform/external/rust/crates/memchr
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/memchr.git