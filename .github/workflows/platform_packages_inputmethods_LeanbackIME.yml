name: platform/packages/inputmethods/LeanbackIME Clone and Push

on:
  schedule:
    - cron: '7 18 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/inputmethods/LeanbackIME platform/packages/inputmethods/LeanbackIME

    - name: Push
      run: |
        cd platform/packages/inputmethods/LeanbackIME
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/inputmethods/LeanbackIME.git