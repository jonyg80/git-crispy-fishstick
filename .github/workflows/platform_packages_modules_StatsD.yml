name: platform/packages/modules/StatsD Clone and Push

on:
  schedule:
    - cron: '9 0 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/StatsD platform/packages/modules/StatsD

    - name: Push
      run: |
        cd platform/packages/modules/StatsD
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/modules/StatsD.git