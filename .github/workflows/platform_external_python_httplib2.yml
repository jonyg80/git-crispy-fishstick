name: platform/external/python/httplib2 Clone and Push

on:
  schedule:
    - cron: '3 1 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/httplib2 platform/external/python/httplib2

    - name: Push
      run: |
        cd platform/external/python/httplib2
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/python/httplib2.git