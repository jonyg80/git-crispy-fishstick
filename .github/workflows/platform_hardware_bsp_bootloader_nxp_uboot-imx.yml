name: platform/hardware/bsp/bootloader/nxp/uboot-imx Clone and Push

on:
  schedule:
    - cron: '33 13 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/bsp/bootloader/nxp/uboot-imx platform/hardware/bsp/bootloader/nxp/uboot-imx

    - name: Push
      run: |
        cd platform/hardware/bsp/bootloader/nxp/uboot-imx
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/bsp/bootloader/nxp/uboot-imx.git