name: platform/external/jetbrains/jdk8u_corba Clone and Push

on:
  schedule:
    - cron: '27 10 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/jetbrains/jdk8u_corba platform/external/jetbrains/jdk8u_corba

    - name: Push
      run: |
        cd platform/external/jetbrains/jdk8u_corba
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/jetbrains/jdk8u_corba.git