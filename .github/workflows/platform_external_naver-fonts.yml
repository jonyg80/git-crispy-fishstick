name: platform/external/naver-fonts Clone and Push

on:
  schedule:
    - cron: '15 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/naver-fonts platform/external/naver-fonts

    - name: Push
      run: |
        cd platform/external/naver-fonts
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/naver-fonts.git