name: platform/packages/wallpapers/PhaseBeam Clone and Push

on:
  schedule:
    - cron: '45 2 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/wallpapers/PhaseBeam platform/packages/wallpapers/PhaseBeam

    - name: Push
      run: |
        cd platform/packages/wallpapers/PhaseBeam
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/wallpapers/PhaseBeam.git