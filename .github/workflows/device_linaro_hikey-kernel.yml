name: device/linaro/hikey-kernel Clone and Push

on:
  schedule:
    - cron: '45 6 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/device/linaro/hikey-kernel device/linaro/hikey-kernel

    - name: Push
      run: |
        cd device/linaro/hikey-kernel
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/device/linaro/hikey-kernel.git