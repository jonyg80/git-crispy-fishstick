name: platform/frameworks/libs/gsma_services Clone and Push

on:
  schedule:
    - cron: '24 5 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/libs/gsma_services platform/frameworks/libs/gsma_services

    - name: Push
      run: |
        cd platform/frameworks/libs/gsma_services
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/frameworks/libs/gsma_services.git