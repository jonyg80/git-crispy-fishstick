name: platform/packages/apps/SpeechRecorder Clone and Push

on:
  schedule:
    - cron: '42 17 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/SpeechRecorder platform/packages/apps/SpeechRecorder

    - name: Push
      run: |
        cd platform/packages/apps/SpeechRecorder
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/SpeechRecorder.git