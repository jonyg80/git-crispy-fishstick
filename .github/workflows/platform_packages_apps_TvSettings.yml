name: platform/packages/apps/TvSettings Clone and Push

on:
  schedule:
    - cron: '33 21 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/TvSettings platform/packages/apps/TvSettings

    - name: Push
      run: |
        cd platform/packages/apps/TvSettings
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/TvSettings.git