name: platform/packages/apps/QuickSearchBox Clone and Push

on:
  schedule:
    - cron: '31 17 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/QuickSearchBox platform/packages/apps/QuickSearchBox

    - name: Push
      run: |
        cd platform/packages/apps/QuickSearchBox
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/QuickSearchBox.git