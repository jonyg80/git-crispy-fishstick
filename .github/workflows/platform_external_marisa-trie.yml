name: platform/external/marisa-trie Clone and Push

on:
  schedule:
    - cron: '45 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/marisa-trie platform/external/marisa-trie

    - name: Push
      run: |
        cd platform/external/marisa-trie
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/marisa-trie.git