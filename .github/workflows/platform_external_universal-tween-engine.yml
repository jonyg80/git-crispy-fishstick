name: platform/external/universal-tween-engine Clone and Push

on:
  schedule:
    - cron: '31 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/universal-tween-engine platform/external/universal-tween-engine

    - name: Push
      run: |
        cd platform/external/universal-tween-engine
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/universal-tween-engine.git