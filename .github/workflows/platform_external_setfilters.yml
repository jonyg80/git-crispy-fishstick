name: platform/external/setfilters Clone and Push

on:
  schedule:
    - cron: '19 10 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/setfilters platform/external/setfilters

    - name: Push
      run: |
        cd platform/external/setfilters
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/setfilters.git