name: platform/external/vulkan-validation-layers Clone and Push

on:
  schedule:
    - cron: '6 3 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/vulkan-validation-layers platform/external/vulkan-validation-layers

    - name: Push
      run: |
        cd platform/external/vulkan-validation-layers
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/vulkan-validation-layers.git