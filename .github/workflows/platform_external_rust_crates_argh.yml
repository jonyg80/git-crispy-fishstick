name: platform/external/rust/crates/argh Clone and Push

on:
  schedule:
    - cron: '22 4 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/argh platform/external/rust/crates/argh

    - name: Push
      run: |
        cd platform/external/rust/crates/argh
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/argh.git