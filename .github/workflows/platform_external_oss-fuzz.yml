name: platform/external/oss-fuzz Clone and Push

on:
  schedule:
    - cron: '0 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/oss-fuzz platform/external/oss-fuzz

    - name: Push
      run: |
        cd platform/external/oss-fuzz
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/oss-fuzz.git