name: platform/hardware/st/secure_element Clone and Push

on:
  schedule:
    - cron: '0 15 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/st/secure_element platform/hardware/st/secure_element

    - name: Push
      run: |
        cd platform/hardware/st/secure_element
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/st/secure_element.git