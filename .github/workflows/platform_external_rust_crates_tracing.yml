name: platform/external/rust/crates/tracing Clone and Push

on:
  schedule:
    - cron: '30 19 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/tracing platform/external/rust/crates/tracing

    - name: Push
      run: |
        cd platform/external/rust/crates/tracing
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/rust/crates/tracing.git