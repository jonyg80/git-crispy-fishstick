name: platform/hardware/ti/omap4-aah Clone and Push

on:
  schedule:
    - cron: '18 15 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/ti/omap4-aah platform/hardware/ti/omap4-aah

    - name: Push
      run: |
        cd platform/hardware/ti/omap4-aah
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/ti/omap4-aah.git