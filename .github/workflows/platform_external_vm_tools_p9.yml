name: platform/external/vm_tools/p9 Clone and Push

on:
  schedule:
    - cron: '41 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/vm_tools/p9 platform/external/vm_tools/p9

    - name: Push
      run: |
        cd platform/external/vm_tools/p9
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/vm_tools/p9.git