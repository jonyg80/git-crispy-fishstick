name: platform/tools/apkzlib Clone and Push

on:
  schedule:
    - cron: '15 22 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/apkzlib platform/tools/apkzlib

    - name: Push
      run: |
        cd platform/tools/apkzlib
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/tools/apkzlib.git