name: platform/packages/modules/BootPrebuilt/5.4/arm64 Clone and Push

on:
  schedule:
    - cron: '36 22 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/BootPrebuilt/5.4/arm64 platform/packages/modules/BootPrebuilt/5.4/arm64

    - name: Push
      run: |
        cd platform/packages/modules/BootPrebuilt/5.4/arm64
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/modules/BootPrebuilt/5.4/arm64.git