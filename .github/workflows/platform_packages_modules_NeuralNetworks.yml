name: platform/packages/modules/NeuralNetworks Clone and Push

on:
  schedule:
    - cron: '38 18 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/NeuralNetworks platform/packages/modules/NeuralNetworks

    - name: Push
      run: |
        cd platform/packages/modules/NeuralNetworks
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/modules/NeuralNetworks.git