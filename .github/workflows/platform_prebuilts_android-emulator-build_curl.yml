name: platform/prebuilts/android-emulator-build/curl Clone and Push

on:
  schedule:
    - cron: '15 3 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/android-emulator-build/curl platform/prebuilts/android-emulator-build/curl

    - name: Push
      run: |
        cd platform/prebuilts/android-emulator-build/curl
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/android-emulator-build/curl.git