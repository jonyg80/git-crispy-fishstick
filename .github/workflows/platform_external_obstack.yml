name: platform/external/obstack Clone and Push

on:
  schedule:
    - cron: '36 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/obstack platform/external/obstack

    - name: Push
      run: |
        cd platform/external/obstack
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/obstack.git