name: platform/external/wuffs-mirror-release-c Clone and Push

on:
  schedule:
    - cron: '2 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/wuffs-mirror-release-c platform/external/wuffs-mirror-release-c

    - name: Push
      run: |
        cd platform/external/wuffs-mirror-release-c
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/wuffs-mirror-release-c.git