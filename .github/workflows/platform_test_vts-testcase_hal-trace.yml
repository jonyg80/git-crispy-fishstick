name: platform/test/vts-testcase/hal-trace Clone and Push

on:
  schedule:
    - cron: '57 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/test/vts-testcase/hal-trace platform/test/vts-testcase/hal-trace

    - name: Push
      run: |
        cd platform/test/vts-testcase/hal-trace
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/test/vts-testcase/hal-trace.git