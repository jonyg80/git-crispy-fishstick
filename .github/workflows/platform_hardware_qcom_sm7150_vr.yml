name: platform/hardware/qcom/sm7150/vr Clone and Push

on:
  schedule:
    - cron: '12 14 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/qcom/sm7150/vr platform/hardware/qcom/sm7150/vr

    - name: Push
      run: |
        cd platform/hardware/qcom/sm7150/vr
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/qcom/sm7150/vr.git