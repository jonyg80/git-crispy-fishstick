name: platform/libnativehelper Clone and Push

on:
  schedule:
    - cron: '39 15 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/libnativehelper platform/libnativehelper

    - name: Push
      run: |
        cd platform/libnativehelper
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/libnativehelper.git