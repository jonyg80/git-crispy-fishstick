name: platform/prebuilts/libprotobuf/darwin Clone and Push

on:
  schedule:
    - cron: '0 23 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/libprotobuf/darwin platform/prebuilts/libprotobuf/darwin

    - name: Push
      run: |
        cd platform/prebuilts/libprotobuf/darwin
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/libprotobuf/darwin.git