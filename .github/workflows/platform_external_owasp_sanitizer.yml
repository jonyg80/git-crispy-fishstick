name: platform/external/owasp/sanitizer Clone and Push

on:
  schedule:
    - cron: '4 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/owasp/sanitizer platform/external/owasp/sanitizer

    - name: Push
      run: |
        cd platform/external/owasp/sanitizer
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/owasp/sanitizer.git