name: platform/external/openwrt-prebuilts Clone and Push

on:
  schedule:
    - cron: '57 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/openwrt-prebuilts platform/external/openwrt-prebuilts

    - name: Push
      run: |
        cd platform/external/openwrt-prebuilts
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/openwrt-prebuilts.git