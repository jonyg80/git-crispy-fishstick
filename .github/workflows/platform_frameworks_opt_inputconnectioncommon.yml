name: platform/frameworks/opt/inputconnectioncommon Clone and Push

on:
  schedule:
    - cron: '59 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/opt/inputconnectioncommon platform/frameworks/opt/inputconnectioncommon

    - name: Push
      run: |
        cd platform/frameworks/opt/inputconnectioncommon
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/opt/inputconnectioncommon.git