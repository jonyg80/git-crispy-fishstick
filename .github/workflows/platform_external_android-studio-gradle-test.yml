name: platform/external/android-studio-gradle-test Clone and Push

on:
  schedule:
    - cron: '27 17 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/android-studio-gradle-test platform/external/android-studio-gradle-test

    - name: Push
      run: |
        cd platform/external/android-studio-gradle-test
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/android-studio-gradle-test.git