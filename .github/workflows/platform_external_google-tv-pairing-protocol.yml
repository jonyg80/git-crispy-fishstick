name: platform/external/google-tv-pairing-protocol Clone and Push

on:
  schedule:
    - cron: '9 7 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/google-tv-pairing-protocol platform/external/google-tv-pairing-protocol

    - name: Push
      run: |
        cd platform/external/google-tv-pairing-protocol
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/google-tv-pairing-protocol.git