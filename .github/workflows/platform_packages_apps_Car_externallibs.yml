name: platform/packages/apps/Car/externallibs Clone and Push

on:
  schedule:
    - cron: '44 16 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/Car/externallibs platform/packages/apps/Car/externallibs

    - name: Push
      run: |
        cd platform/packages/apps/Car/externallibs
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/Car/externallibs.git