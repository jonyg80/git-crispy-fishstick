name: platform/prebuilts/module_sdk/ThreadNetwork Clone and Push

on:
  schedule:
    - cron: '12 14 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/module_sdk/ThreadNetwork platform/prebuilts/module_sdk/ThreadNetwork

    - name: Push
      run: |
        cd platform/prebuilts/module_sdk/ThreadNetwork
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/module_sdk/ThreadNetwork.git