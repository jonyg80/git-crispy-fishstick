name: platform/external/python/pyopenssl Clone and Push

on:
  schedule:
    - cron: '35 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/pyopenssl platform/external/python/pyopenssl

    - name: Push
      run: |
        cd platform/external/python/pyopenssl
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/python/pyopenssl.git