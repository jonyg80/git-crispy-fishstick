name: platform/frameworks/opt/car/setupwizard Clone and Push

on:
  schedule:
    - cron: '12 6 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/opt/car/setupwizard platform/frameworks/opt/car/setupwizard

    - name: Push
      run: |
        cd platform/frameworks/opt/car/setupwizard
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/frameworks/opt/car/setupwizard.git