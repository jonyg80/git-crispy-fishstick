name: platform/external/google-diff-match-patch Clone and Push

on:
  schedule:
    - cron: '9 6 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/google-diff-match-patch platform/external/google-diff-match-patch

    - name: Push
      run: |
        cd platform/external/google-diff-match-patch
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/google-diff-match-patch.git