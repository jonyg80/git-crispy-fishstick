name: device/google/accessory/demokit Clone and Push

on:
  schedule:
    - cron: '6 2 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/device/google/accessory/demokit device/google/accessory/demokit

    - name: Push
      run: |
        cd device/google/accessory/demokit
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/device/google/accessory/demokit.git