name: trusty/external/trusted-firmware-a Clone and Push

on:
  schedule:
    - cron: '11 5 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/trusty/external/trusted-firmware-a trusty/external/trusted-firmware-a

    - name: Push
      run: |
        cd trusty/external/trusted-firmware-a
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/trusty/external/trusted-firmware-a.git