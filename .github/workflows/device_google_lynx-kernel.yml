name: device/google/lynx-kernel Clone and Push

on:
  schedule:
    - cron: '3 4 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/device/google/lynx-kernel device/google/lynx-kernel

    - name: Push
      run: |
        cd device/google/lynx-kernel
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/device/google/lynx-kernel.git