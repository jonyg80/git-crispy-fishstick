name: platform/prebuilts/libprotobuf/linux Clone and Push

on:
  schedule:
    - cron: '2 23 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/libprotobuf/linux platform/prebuilts/libprotobuf/linux

    - name: Push
      run: |
        cd platform/prebuilts/libprotobuf/linux
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/libprotobuf/linux.git