name: platform/external/tinyalsa_new Clone and Push

on:
  schedule:
    - cron: '11 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/tinyalsa_new platform/external/tinyalsa_new

    - name: Push
      run: |
        cd platform/external/tinyalsa_new
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/tinyalsa_new.git