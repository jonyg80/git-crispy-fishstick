name: platform/hardware/google/graphics/gs201 Clone and Push

on:
  schedule:
    - cron: '36 10 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/google/graphics/gs201 platform/hardware/google/graphics/gs201

    - name: Push
      run: |
        cd platform/hardware/google/graphics/gs201
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/google/graphics/gs201.git