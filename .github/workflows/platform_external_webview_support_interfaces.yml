name: platform/external/webview_support_interfaces Clone and Push

on:
  schedule:
    - cron: '33 3 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/webview_support_interfaces platform/external/webview_support_interfaces

    - name: Push
      run: |
        cd platform/external/webview_support_interfaces
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/webview_support_interfaces.git