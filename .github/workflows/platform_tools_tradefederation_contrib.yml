name: platform/tools/tradefederation/contrib Clone and Push

on:
  schedule:
    - cron: '11 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/tradefederation/contrib platform/tools/tradefederation/contrib

    - name: Push
      run: |
        cd platform/tools/tradefederation/contrib
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/tools/tradefederation/contrib.git