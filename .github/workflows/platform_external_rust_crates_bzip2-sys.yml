name: platform/external/rust/crates/bzip2-sys Clone and Push

on:
  schedule:
    - cron: '47 4 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/bzip2-sys platform/external/rust/crates/bzip2-sys

    - name: Push
      run: |
        cd platform/external/rust/crates/bzip2-sys
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/bzip2-sys.git