name: platform/external/kotlinx.metadata Clone and Push

on:
  schedule:
    - cron: '54 11 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/kotlinx.metadata platform/external/kotlinx.metadata

    - name: Push
      run: |
        cd platform/external/kotlinx.metadata
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/kotlinx.metadata.git