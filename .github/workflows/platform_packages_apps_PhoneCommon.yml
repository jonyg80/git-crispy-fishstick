name: platform/packages/apps/PhoneCommon Clone and Push

on:
  schedule:
    - cron: '6 20 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/PhoneCommon platform/packages/apps/PhoneCommon

    - name: Push
      run: |
        cd platform/packages/apps/PhoneCommon
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/PhoneCommon.git