name: platform/packages/apps/Car/MultiDisplay Clone and Push

on:
  schedule:
    - cron: '18 17 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/Car/MultiDisplay platform/packages/apps/Car/MultiDisplay

    - name: Push
      run: |
        cd platform/packages/apps/Car/MultiDisplay
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/Car/MultiDisplay.git