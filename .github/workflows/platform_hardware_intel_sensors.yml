name: platform/hardware/intel/sensors Clone and Push

on:
  schedule:
    - cron: '33 11 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/intel/sensors platform/hardware/intel/sensors

    - name: Push
      run: |
        cd platform/hardware/intel/sensors
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/intel/sensors.git