name: platform/external/bazelbuild-rules_cc Clone and Push

on:
  schedule:
    - cron: '33 19 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/bazelbuild-rules_cc platform/external/bazelbuild-rules_cc

    - name: Push
      run: |
        cd platform/external/bazelbuild-rules_cc
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/bazelbuild-rules_cc.git