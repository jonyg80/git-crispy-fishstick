name: platform/prebuilts/fullsdk/platforms/android-32 Clone and Push

on:
  schedule:
    - cron: '21 8 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/fullsdk/platforms/android-32 platform/prebuilts/fullsdk/platforms/android-32

    - name: Push
      run: |
        cd platform/prebuilts/fullsdk/platforms/android-32
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/fullsdk/platforms/android-32.git