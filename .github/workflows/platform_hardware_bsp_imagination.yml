name: platform/hardware/bsp/imagination Clone and Push

on:
  schedule:
    - cron: '37 13 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/bsp/imagination platform/hardware/bsp/imagination

    - name: Push
      run: |
        cd platform/hardware/bsp/imagination
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/bsp/imagination.git