name: platform/external/rust/crates/openssl-macros Clone and Push

on:
  schedule:
    - cron: '30 13 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/openssl-macros platform/external/rust/crates/openssl-macros

    - name: Push
      run: |
        cd platform/external/rust/crates/openssl-macros
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/rust/crates/openssl-macros.git