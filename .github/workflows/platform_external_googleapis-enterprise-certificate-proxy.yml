name: platform/external/googleapis-enterprise-certificate-proxy Clone and Push

on:
  schedule:
    - cron: '18 7 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/googleapis-enterprise-certificate-proxy platform/external/googleapis-enterprise-certificate-proxy

    - name: Push
      run: |
        cd platform/external/googleapis-enterprise-certificate-proxy
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/googleapis-enterprise-certificate-proxy.git