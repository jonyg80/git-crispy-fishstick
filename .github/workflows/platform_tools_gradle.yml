name: platform/tools/gradle Clone and Push

on:
  schedule:
    - cron: '39 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/gradle platform/tools/gradle

    - name: Push
      run: |
        cd platform/tools/gradle
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/tools/gradle.git