name: platform/packages/modules/Sharesheet Clone and Push

on:
  schedule:
    - cron: '6 0 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/Sharesheet platform/packages/modules/Sharesheet

    - name: Push
      run: |
        cd platform/packages/modules/Sharesheet
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/modules/Sharesheet.git