name: platform/external/rust/crates/rustc_version Clone and Push

on:
  schedule:
    - cron: '33 16 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/rustc_version platform/external/rust/crates/rustc_version

    - name: Push
      run: |
        cd platform/external/rust/crates/rustc_version
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/rust/crates/rustc_version.git