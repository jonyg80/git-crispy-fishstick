name: platform/external/erofs-utils Clone and Push

on:
  schedule:
    - cron: '54 2 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/erofs-utils platform/external/erofs-utils

    - name: Push
      run: |
        cd platform/external/erofs-utils
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/erofs-utils.git