name: platform/hardware/qcom/sdm845/thermal Clone and Push

on:
  schedule:
    - cron: '54 13 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/qcom/sdm845/thermal platform/hardware/qcom/sdm845/thermal

    - name: Push
      run: |
        cd platform/hardware/qcom/sdm845/thermal
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/qcom/sdm845/thermal.git