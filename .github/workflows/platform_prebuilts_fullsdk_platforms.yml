name: platform/prebuilts/fullsdk/platforms Clone and Push

on:
  schedule:
    - cron: '25 21 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/fullsdk/platforms platform/prebuilts/fullsdk/platforms

    - name: Push
      run: |
        cd platform/prebuilts/fullsdk/platforms
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/fullsdk/platforms.git