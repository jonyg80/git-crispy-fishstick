name: platform/prebuilts/module_sdk/Bluetooth Clone and Push

on:
  schedule:
    - cron: '14 23 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/module_sdk/Bluetooth platform/prebuilts/module_sdk/Bluetooth

    - name: Push
      run: |
        cd platform/prebuilts/module_sdk/Bluetooth
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/module_sdk/Bluetooth.git