name: platform/packages/wallpapers/NoiseField Clone and Push

on:
  schedule:
    - cron: '39 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/wallpapers/NoiseField platform/packages/wallpapers/NoiseField

    - name: Push
      run: |
        cd platform/packages/wallpapers/NoiseField
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/wallpapers/NoiseField.git