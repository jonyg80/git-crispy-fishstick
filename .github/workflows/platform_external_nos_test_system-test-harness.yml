name: platform/external/nos/test/system-test-harness Clone and Push

on:
  schedule:
    - cron: '9 20 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/nos/test/system-test-harness platform/external/nos/test/system-test-harness

    - name: Push
      run: |
        cd platform/external/nos/test/system-test-harness
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/nos/test/system-test-harness.git