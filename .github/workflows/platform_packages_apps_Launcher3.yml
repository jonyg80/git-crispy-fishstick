name: platform/packages/apps/Launcher3 Clone and Push

on:
  schedule:
    - cron: '15 17 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/Launcher3 platform/packages/apps/Launcher3

    - name: Push
      run: |
        cd platform/packages/apps/Launcher3
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/Launcher3.git