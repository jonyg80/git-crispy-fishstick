name: platform/external/rust/pica Clone and Push

on:
  schedule:
    - cron: '5 10 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/pica platform/external/rust/pica

    - name: Push
      run: |
        cd platform/external/rust/pica
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/pica.git