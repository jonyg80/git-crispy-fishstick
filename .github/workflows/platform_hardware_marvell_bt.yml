name: platform/hardware/marvell/bt Clone and Push

on:
  schedule:
    - cron: '51 11 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/marvell/bt platform/hardware/marvell/bt

    - name: Push
      run: |
        cd platform/hardware/marvell/bt
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/marvell/bt.git