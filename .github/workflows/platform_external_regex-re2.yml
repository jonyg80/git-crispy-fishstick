name: platform/external/regex-re2 Clone and Push

on:
  schedule:
    - cron: '57 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/regex-re2 platform/external/regex-re2

    - name: Push
      run: |
        cd platform/external/regex-re2
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/regex-re2.git