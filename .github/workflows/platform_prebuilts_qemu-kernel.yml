name: platform/prebuilts/qemu-kernel Clone and Push

on:
  schedule:
    - cron: '44 23 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/qemu-kernel platform/prebuilts/qemu-kernel

    - name: Push
      run: |
        cd platform/prebuilts/qemu-kernel
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/qemu-kernel.git