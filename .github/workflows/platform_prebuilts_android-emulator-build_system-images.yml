name: platform/prebuilts/android-emulator-build/system-images Clone and Push

on:
  schedule:
    - cron: '33 3 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/android-emulator-build/system-images platform/prebuilts/android-emulator-build/system-images

    - name: Push
      run: |
        cd platform/prebuilts/android-emulator-build/system-images
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/android-emulator-build/system-images.git