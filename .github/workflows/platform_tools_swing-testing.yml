name: platform/tools/swing-testing Clone and Push

on:
  schedule:
    - cron: '2 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/swing-testing platform/tools/swing-testing

    - name: Push
      run: |
        cd platform/tools/swing-testing
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/tools/swing-testing.git