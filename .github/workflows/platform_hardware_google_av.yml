name: platform/hardware/google/av Clone and Push

on:
  schedule:
    - cron: '10 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/google/av platform/hardware/google/av

    - name: Push
      run: |
        cd platform/hardware/google/av
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/google/av.git