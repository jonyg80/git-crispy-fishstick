name: platform/frameworks/opt/net/ims Clone and Push

on:
  schedule:
    - cron: '6 13 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/opt/net/ims platform/frameworks/opt/net/ims

    - name: Push
      run: |
        cd platform/frameworks/opt/net/ims
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/opt/net/ims.git