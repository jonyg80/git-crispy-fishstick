name: platform/frameworks/opt/colorpicker Clone and Push

on:
  schedule:
    - cron: '52 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/opt/colorpicker platform/frameworks/opt/colorpicker

    - name: Push
      run: |
        cd platform/frameworks/opt/colorpicker
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/opt/colorpicker.git