name: platform/packages/apps/QuickAccessWallet Clone and Push

on:
  schedule:
    - cron: '15 20 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/QuickAccessWallet platform/packages/apps/QuickAccessWallet

    - name: Push
      run: |
        cd platform/packages/apps/QuickAccessWallet
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/QuickAccessWallet.git