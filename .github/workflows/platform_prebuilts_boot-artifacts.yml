name: platform/prebuilts/boot-artifacts Clone and Push

on:
  schedule:
    - cron: '12 4 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/boot-artifacts platform/prebuilts/boot-artifacts

    - name: Push
      run: |
        cd platform/prebuilts/boot-artifacts
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/boot-artifacts.git