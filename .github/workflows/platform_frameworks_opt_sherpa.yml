name: platform/frameworks/opt/sherpa Clone and Push

on:
  schedule:
    - cron: '21 7 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/opt/sherpa platform/frameworks/opt/sherpa

    - name: Push
      run: |
        cd platform/frameworks/opt/sherpa
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/frameworks/opt/sherpa.git