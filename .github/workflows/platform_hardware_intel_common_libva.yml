name: platform/hardware/intel/common/libva Clone and Push

on:
  schedule:
    - cron: '6 11 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/intel/common/libva platform/hardware/intel/common/libva

    - name: Push
      run: |
        cd platform/hardware/intel/common/libva
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/intel/common/libva.git