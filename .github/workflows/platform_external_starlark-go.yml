name: platform/external/starlark-go Clone and Push

on:
  schedule:
    - cron: '48 10 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/starlark-go platform/external/starlark-go

    - name: Push
      run: |
        cd platform/external/starlark-go
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/starlark-go.git