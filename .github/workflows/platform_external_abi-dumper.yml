name: platform/external/abi-dumper Clone and Push

on:
  schedule:
    - cron: '45 16 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/abi-dumper platform/external/abi-dumper

    - name: Push
      run: |
        cd platform/external/abi-dumper
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/abi-dumper.git