name: kernel/devices/google/lynx Clone and Push

on:
  schedule:
    - cron: '51 8 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/kernel/devices/google/lynx kernel/devices/google/lynx

    - name: Push
      run: |
        cd kernel/devices/google/lynx
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/kernel/devices/google/lynx.git