name: platform/tools/apksig Clone and Push

on:
  schedule:
    - cron: '9 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/apksig platform/tools/apksig

    - name: Push
      run: |
        cd platform/tools/apksig
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/tools/apksig.git