name: trusty/external/qemu-libslirp Clone and Push

on:
  schedule:
    - cron: '9 5 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/trusty/external/qemu-libslirp trusty/external/qemu-libslirp

    - name: Push
      run: |
        cd trusty/external/qemu-libslirp
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/trusty/external/qemu-libslirp.git