name: platform/hardware/intel/common/wrs_omxil_core Clone and Push

on:
  schedule:
    - cron: '31 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/intel/common/wrs_omxil_core platform/hardware/intel/common/wrs_omxil_core

    - name: Push
      run: |
        cd platform/hardware/intel/common/wrs_omxil_core
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/intel/common/wrs_omxil_core.git