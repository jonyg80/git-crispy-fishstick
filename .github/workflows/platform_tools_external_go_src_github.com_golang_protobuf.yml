name: platform/tools/external/go/src/github.com/golang/protobuf Clone and Push

on:
  schedule:
    - cron: '32 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/external/go/src/github.com/golang/protobuf platform/tools/external/go/src/github.com/golang/protobuf

    - name: Push
      run: |
        cd platform/tools/external/go/src/github.com/golang/protobuf
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/tools/external/go/src/github.com/golang/protobuf.git