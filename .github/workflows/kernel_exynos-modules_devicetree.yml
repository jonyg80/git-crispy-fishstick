name: kernel/exynos-modules/devicetree Clone and Push

on:
  schedule:
    - cron: '3 9 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/kernel/exynos-modules/devicetree kernel/exynos-modules/devicetree

    - name: Push
      run: |
        cd kernel/exynos-modules/devicetree
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/kernel/exynos-modules/devicetree.git