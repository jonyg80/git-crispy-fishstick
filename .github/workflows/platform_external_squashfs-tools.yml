name: platform/external/squashfs-tools Clone and Push

on:
  schedule:
    - cron: '44 10 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/squashfs-tools platform/external/squashfs-tools

    - name: Push
      run: |
        cd platform/external/squashfs-tools
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/squashfs-tools.git