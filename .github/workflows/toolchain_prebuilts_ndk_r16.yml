name: toolchain/prebuilts/ndk/r16 Clone and Push

on:
  schedule:
    - cron: '57 4 * * 5' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/toolchain/prebuilts/ndk/r16 toolchain/prebuilts/ndk/r16

    - name: Push
      run: |
        cd toolchain/prebuilts/ndk/r16
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/toolchain/prebuilts/ndk/r16.git