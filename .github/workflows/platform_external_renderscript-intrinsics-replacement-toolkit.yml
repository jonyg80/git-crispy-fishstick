name: platform/external/renderscript-intrinsics-replacement-toolkit Clone and Push

on:
  schedule:
    - cron: '39 3 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/renderscript-intrinsics-replacement-toolkit platform/external/renderscript-intrinsics-replacement-toolkit

    - name: Push
      run: |
        cd platform/external/renderscript-intrinsics-replacement-toolkit
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/renderscript-intrinsics-replacement-toolkit.git