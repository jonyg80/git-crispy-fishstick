name: platform/external/shaderc/shaderc Clone and Push

on:
  schedule:
    - cron: '0 23 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/shaderc/shaderc platform/external/shaderc/shaderc

    - name: Push
      run: |
        cd platform/external/shaderc/shaderc
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/shaderc/shaderc.git