name: platform/packages/providers/UserDictionaryProvider Clone and Push

on:
  schedule:
    - cron: '15 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/providers/UserDictionaryProvider platform/packages/providers/UserDictionaryProvider

    - name: Push
      run: |
        cd platform/packages/providers/UserDictionaryProvider
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/providers/UserDictionaryProvider.git