name: platform/hardware/qcom/data/ipacfg-mgr Clone and Push

on:
  schedule:
    - cron: '56 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/qcom/data/ipacfg-mgr platform/hardware/qcom/data/ipacfg-mgr

    - name: Push
      run: |
        cd platform/hardware/qcom/data/ipacfg-mgr
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/qcom/data/ipacfg-mgr.git