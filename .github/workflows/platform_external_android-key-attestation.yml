name: platform/external/android-key-attestation Clone and Push

on:
  schedule:
    - cron: '15 17 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/android-key-attestation platform/external/android-key-attestation

    - name: Push
      run: |
        cd platform/external/android-key-attestation
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/android-key-attestation.git