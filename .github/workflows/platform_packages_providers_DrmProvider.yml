name: platform/packages/providers/DrmProvider Clone and Push

on:
  schedule:
    - cron: '6 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/providers/DrmProvider platform/packages/providers/DrmProvider

    - name: Push
      run: |
        cd platform/packages/providers/DrmProvider
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/providers/DrmProvider.git