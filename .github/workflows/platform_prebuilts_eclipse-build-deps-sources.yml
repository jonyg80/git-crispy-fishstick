name: platform/prebuilts/eclipse-build-deps-sources Clone and Push

on:
  schedule:
    - cron: '51 20 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/eclipse-build-deps-sources platform/prebuilts/eclipse-build-deps-sources

    - name: Push
      run: |
        cd platform/prebuilts/eclipse-build-deps-sources
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/eclipse-build-deps-sources.git