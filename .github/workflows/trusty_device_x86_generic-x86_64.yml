name: trusty/device/x86/generic-x86_64 Clone and Push

on:
  schedule:
    - cron: '4 5 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/trusty/device/x86/generic-x86_64 trusty/device/x86/generic-x86_64

    - name: Push
      run: |
        cd trusty/device/x86/generic-x86_64
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/trusty/device/x86/generic-x86_64.git