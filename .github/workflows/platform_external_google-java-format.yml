name: platform/external/google-java-format Clone and Push

on:
  schedule:
    - cron: '0 7 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/google-java-format platform/external/google-java-format

    - name: Push
      run: |
        cd platform/external/google-java-format
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/google-java-format.git