name: platform/hardware/qcom/sm8150/thermal Clone and Push

on:
  schedule:
    - cron: '37 15 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/qcom/sm8150/thermal platform/hardware/qcom/sm8150/thermal

    - name: Push
      run: |
        cd platform/hardware/qcom/sm8150/thermal
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/qcom/sm8150/thermal.git