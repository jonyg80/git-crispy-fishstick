name: platform/external/noto-fonts Clone and Push

on:
  schedule:
    - cron: '29 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/noto-fonts platform/external/noto-fonts

    - name: Push
      run: |
        cd platform/external/noto-fonts
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/noto-fonts.git