name: platform/tools/trebuchet Clone and Push

on:
  schedule:
    - cron: '27 1 * * 5' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/trebuchet platform/tools/trebuchet

    - name: Push
      run: |
        cd platform/tools/trebuchet
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/tools/trebuchet.git