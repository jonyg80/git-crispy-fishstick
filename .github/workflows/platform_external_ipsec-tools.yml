name: platform/external/ipsec-tools Clone and Push

on:
  schedule:
    - cron: '12 9 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/ipsec-tools platform/external/ipsec-tools

    - name: Push
      run: |
        cd platform/external/ipsec-tools
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/ipsec-tools.git