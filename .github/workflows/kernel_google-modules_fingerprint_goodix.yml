name: kernel/google-modules/fingerprint/goodix Clone and Push

on:
  schedule:
    - cron: '51 9 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/kernel/google-modules/fingerprint/goodix kernel/google-modules/fingerprint/goodix

    - name: Push
      run: |
        cd kernel/google-modules/fingerprint/goodix
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/kernel/google-modules/fingerprint/goodix.git