name: platform/hardware/ti/omap4xxx Clone and Push

on:
  schedule:
    - cron: '52 15 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/ti/omap4xxx platform/hardware/ti/omap4xxx

    - name: Push
      run: |
        cd platform/hardware/ti/omap4xxx
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/ti/omap4xxx.git