name: platform/external/mobly-snippet-lib Clone and Push

on:
  schedule:
    - cron: '39 18 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/mobly-snippet-lib platform/external/mobly-snippet-lib

    - name: Push
      run: |
        cd platform/external/mobly-snippet-lib
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/mobly-snippet-lib.git