name: platform/frameworks/hardware/interfaces Clone and Push

on:
  schedule:
    - cron: '12 5 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/hardware/interfaces platform/frameworks/hardware/interfaces

    - name: Push
      run: |
        cd platform/frameworks/hardware/interfaces
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/frameworks/hardware/interfaces.git