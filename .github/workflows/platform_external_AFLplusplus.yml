name: platform/external/AFLplusplus Clone and Push

on:
  schedule:
    - cron: '36 15 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/AFLplusplus platform/external/AFLplusplus

    - name: Push
      run: |
        cd platform/external/AFLplusplus
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/AFLplusplus.git