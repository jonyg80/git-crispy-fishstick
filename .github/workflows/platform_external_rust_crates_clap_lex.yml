name: platform/external/rust/crates/clap_lex Clone and Push

on:
  schedule:
    - cron: '0 5 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/clap_lex platform/external/rust/crates/clap_lex

    - name: Push
      run: |
        cd platform/external/rust/crates/clap_lex
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/clap_lex.git