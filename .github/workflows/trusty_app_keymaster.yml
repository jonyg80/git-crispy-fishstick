name: trusty/app/keymaster Clone and Push

on:
  schedule:
    - cron: '54 4 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/trusty/app/keymaster trusty/app/keymaster

    - name: Push
      run: |
        cd trusty/app/keymaster
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/trusty/app/keymaster.git