name: platform/external/python/appdirs Clone and Push

on:
  schedule:
    - cron: '36 23 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/appdirs platform/external/python/appdirs

    - name: Push
      run: |
        cd platform/external/python/appdirs
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/python/appdirs.git