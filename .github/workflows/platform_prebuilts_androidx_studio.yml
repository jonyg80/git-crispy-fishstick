name: platform/prebuilts/androidx/studio Clone and Push

on:
  schedule:
    - cron: '48 3 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/androidx/studio platform/prebuilts/androidx/studio

    - name: Push
      run: |
        cd platform/prebuilts/androidx/studio
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/androidx/studio.git