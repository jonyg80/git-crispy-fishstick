name: platform/system/webservd Clone and Push

on:
  schedule:
    - cron: '34 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/webservd platform/system/webservd

    - name: Push
      run: |
        cd platform/system/webservd
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/webservd.git