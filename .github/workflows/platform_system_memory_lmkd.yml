name: platform/system/memory/lmkd Clone and Push

on:
  schedule:
    - cron: '6 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/memory/lmkd platform/system/memory/lmkd

    - name: Push
      run: |
        cd platform/system/memory/lmkd
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/memory/lmkd.git