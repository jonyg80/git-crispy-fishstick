name: platform/external/gradle-perf-android-large Clone and Push

on:
  schedule:
    - cron: '36 7 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/gradle-perf-android-large platform/external/gradle-perf-android-large

    - name: Push
      run: |
        cd platform/external/gradle-perf-android-large
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/gradle-perf-android-large.git