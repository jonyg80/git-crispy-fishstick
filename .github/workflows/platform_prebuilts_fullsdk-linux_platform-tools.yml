name: platform/prebuilts/fullsdk-linux/platform-tools Clone and Push

on:
  schedule:
    - cron: '18 21 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/fullsdk-linux/platform-tools platform/prebuilts/fullsdk-linux/platform-tools

    - name: Push
      run: |
        cd platform/prebuilts/fullsdk-linux/platform-tools
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/fullsdk-linux/platform-tools.git