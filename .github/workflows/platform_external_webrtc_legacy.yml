name: platform/external/webrtc_legacy Clone and Push

on:
  schedule:
    - cron: '30 3 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/webrtc_legacy platform/external/webrtc_legacy

    - name: Push
      run: |
        cd platform/external/webrtc_legacy
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/webrtc_legacy.git