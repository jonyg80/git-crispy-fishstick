name: platform/system/connectivity/wifilogd Clone and Push

on:
  schedule:
    - cron: '28 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/connectivity/wifilogd platform/system/connectivity/wifilogd

    - name: Push
      run: |
        cd platform/system/connectivity/wifilogd
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/connectivity/wifilogd.git