name: platform/external/wpa_supplicant_6 Clone and Push

on:
  schedule:
    - cron: '59 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/wpa_supplicant_6 platform/external/wpa_supplicant_6

    - name: Push
      run: |
        cd platform/external/wpa_supplicant_6
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/wpa_supplicant_6.git