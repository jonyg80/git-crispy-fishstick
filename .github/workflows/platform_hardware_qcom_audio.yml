name: platform/hardware/qcom/audio Clone and Push

on:
  schedule:
    - cron: '52 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/qcom/audio platform/hardware/qcom/audio

    - name: Push
      run: |
        cd platform/hardware/qcom/audio
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/qcom/audio.git