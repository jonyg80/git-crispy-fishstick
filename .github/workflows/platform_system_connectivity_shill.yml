name: platform/system/connectivity/shill Clone and Push

on:
  schedule:
    - cron: '26 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/connectivity/shill platform/system/connectivity/shill

    - name: Push
      run: |
        cd platform/system/connectivity/shill
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/connectivity/shill.git