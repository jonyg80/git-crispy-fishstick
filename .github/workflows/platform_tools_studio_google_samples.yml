name: platform/tools/studio/google/samples Clone and Push

on:
  schedule:
    - cron: '42 0 * * 5' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/studio/google/samples platform/tools/studio/google/samples

    - name: Push
      run: |
        cd platform/tools/studio/google/samples
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/tools/studio/google/samples.git