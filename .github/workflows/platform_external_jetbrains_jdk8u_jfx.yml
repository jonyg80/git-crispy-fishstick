name: platform/external/jetbrains/jdk8u_jfx Clone and Push

on:
  schedule:
    - cron: '42 10 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/jetbrains/jdk8u_jfx platform/external/jetbrains/jdk8u_jfx

    - name: Push
      run: |
        cd platform/external/jetbrains/jdk8u_jfx
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/jetbrains/jdk8u_jfx.git