name: platform/test/catbox Clone and Push

on:
  schedule:
    - cron: '39 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/test/catbox platform/test/catbox

    - name: Push
      run: |
        cd platform/test/catbox
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/test/catbox.git