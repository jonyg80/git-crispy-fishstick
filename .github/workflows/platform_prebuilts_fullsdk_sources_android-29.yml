name: platform/prebuilts/fullsdk/sources/android-29 Clone and Push

on:
  schedule:
    - cron: '42 8 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/fullsdk/sources/android-29 platform/prebuilts/fullsdk/sources/android-29

    - name: Push
      run: |
        cd platform/prebuilts/fullsdk/sources/android-29
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/fullsdk/sources/android-29.git