name: platform/prebuilts/trusty/sysroot Clone and Push

on:
  schedule:
    - cron: '0 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/trusty/sysroot platform/prebuilts/trusty/sysroot

    - name: Push
      run: |
        cd platform/prebuilts/trusty/sysroot
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/trusty/sysroot.git