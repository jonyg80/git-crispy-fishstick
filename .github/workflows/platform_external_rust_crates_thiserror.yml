name: platform/external/rust/crates/thiserror Clone and Push

on:
  schedule:
    - cron: '54 8 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/thiserror platform/external/rust/crates/thiserror

    - name: Push
      run: |
        cd platform/external/rust/crates/thiserror
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/thiserror.git