name: platform/external/pixman Clone and Push

on:
  schedule:
    - cron: '20 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/pixman platform/external/pixman

    - name: Push
      run: |
        cd platform/external/pixman
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/pixman.git