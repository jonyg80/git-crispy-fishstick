name: platform/external/rust/crates/which Clone and Push

on:
  schedule:
    - cron: '52 9 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/which platform/external/rust/crates/which

    - name: Push
      run: |
        cd platform/external/rust/crates/which
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/which.git