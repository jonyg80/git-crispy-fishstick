name: platform/external/grpc-grpc-go Clone and Push

on:
  schedule:
    - cron: '45 7 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/grpc-grpc-go platform/external/grpc-grpc-go

    - name: Push
      run: |
        cd platform/external/grpc-grpc-go
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/grpc-grpc-go.git