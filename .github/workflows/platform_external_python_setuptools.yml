name: platform/external/python/setuptools Clone and Push

on:
  schedule:
    - cron: '57 2 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/setuptools platform/external/python/setuptools

    - name: Push
      run: |
        cd platform/external/python/setuptools
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/python/setuptools.git