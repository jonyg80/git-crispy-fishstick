name: platform/external/googleapis-gax-go Clone and Push

on:
  schedule:
    - cron: '21 7 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/googleapis-gax-go platform/external/googleapis-gax-go

    - name: Push
      run: |
        cd platform/external/googleapis-gax-go
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/googleapis-gax-go.git