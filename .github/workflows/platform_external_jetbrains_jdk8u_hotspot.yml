name: platform/external/jetbrains/jdk8u_hotspot Clone and Push

on:
  schedule:
    - cron: '30 10 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/jetbrains/jdk8u_hotspot platform/external/jetbrains/jdk8u_hotspot

    - name: Push
      run: |
        cd platform/external/jetbrains/jdk8u_hotspot
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/jetbrains/jdk8u_hotspot.git