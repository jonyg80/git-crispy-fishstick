name: platform/system/testing/gtest_extras Clone and Push

on:
  schedule:
    - cron: '18 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/testing/gtest_extras platform/system/testing/gtest_extras

    - name: Push
      run: |
        cd platform/system/testing/gtest_extras
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/testing/gtest_extras.git