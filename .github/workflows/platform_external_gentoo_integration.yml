name: platform/external/gentoo/integration Clone and Push

on:
  schedule:
    - cron: '45 4 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/gentoo/integration platform/external/gentoo/integration

    - name: Push
      run: |
        cd platform/external/gentoo/integration
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/gentoo/integration.git