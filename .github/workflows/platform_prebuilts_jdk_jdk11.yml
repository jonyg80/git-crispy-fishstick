name: platform/prebuilts/jdk/jdk11 Clone and Push

on:
  schedule:
    - cron: '50 22 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/jdk/jdk11 platform/prebuilts/jdk/jdk11

    - name: Push
      run: |
        cd platform/prebuilts/jdk/jdk11
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/jdk/jdk11.git