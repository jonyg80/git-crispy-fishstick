name: platform/packages/apps/RetailDemo Clone and Push

on:
  schedule:
    - cron: '33 17 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/RetailDemo platform/packages/apps/RetailDemo

    - name: Push
      run: |
        cd platform/packages/apps/RetailDemo
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/RetailDemo.git