name: platform/frameworks/support-golden Clone and Push

on:
  schedule:
    - cron: '22 13 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/support-golden platform/frameworks/support-golden

    - name: Push
      run: |
        cd platform/frameworks/support-golden
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/support-golden.git