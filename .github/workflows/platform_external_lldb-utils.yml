name: platform/external/lldb-utils Clone and Push

on:
  schedule:
    - cron: '31 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/lldb-utils platform/external/lldb-utils

    - name: Push
      run: |
        cd platform/external/lldb-utils
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/lldb-utils.git