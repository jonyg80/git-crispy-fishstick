name: platform/frameworks/minikin Clone and Push

on:
  schedule:
    - cron: '41 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/minikin platform/frameworks/minikin

    - name: Push
      run: |
        cd platform/frameworks/minikin
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/minikin.git