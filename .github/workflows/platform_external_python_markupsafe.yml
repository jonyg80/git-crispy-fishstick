name: platform/external/python/markupsafe Clone and Push

on:
  schedule:
    - cron: '9 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/markupsafe platform/external/python/markupsafe

    - name: Push
      run: |
        cd platform/external/python/markupsafe
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/python/markupsafe.git