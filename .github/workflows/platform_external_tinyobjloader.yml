name: platform/external/tinyobjloader Clone and Push

on:
  schedule:
    - cron: '13 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/tinyobjloader platform/external/tinyobjloader

    - name: Push
      run: |
        cd platform/external/tinyobjloader
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/tinyobjloader.git