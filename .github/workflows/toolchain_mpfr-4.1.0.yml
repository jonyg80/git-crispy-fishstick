name: toolchain/mpfr-4.1.0 Clone and Push

on:
  schedule:
    - cron: '10 4 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/toolchain/mpfr-4.1.0 toolchain/mpfr-4.1.0

    - name: Push
      run: |
        cd toolchain/mpfr-4.1.0
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/toolchain/mpfr-4.1.0.git