name: platform/external/rust/crates/der-parser Clone and Push

on:
  schedule:
    - cron: '26 5 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/der-parser platform/external/rust/crates/der-parser

    - name: Push
      run: |
        cd platform/external/rust/crates/der-parser
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/der-parser.git