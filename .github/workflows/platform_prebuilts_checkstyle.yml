name: platform/prebuilts/checkstyle Clone and Push

on:
  schedule:
    - cron: '16 20 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/checkstyle platform/prebuilts/checkstyle

    - name: Push
      run: |
        cd platform/prebuilts/checkstyle
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/checkstyle.git