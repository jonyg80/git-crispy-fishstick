name: platform/external/mbedtls Clone and Push

on:
  schedule:
    - cron: '47 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/mbedtls platform/external/mbedtls

    - name: Push
      run: |
        cd platform/external/mbedtls
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/mbedtls.git