name: platform/external/python/pyasn1-modules Clone and Push

on:
  schedule:
    - cron: '12 2 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/pyasn1-modules platform/external/python/pyasn1-modules

    - name: Push
      run: |
        cd platform/external/python/pyasn1-modules
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/python/pyasn1-modules.git