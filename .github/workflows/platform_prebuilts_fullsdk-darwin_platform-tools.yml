name: platform/prebuilts/fullsdk-darwin/platform-tools Clone and Push

on:
  schedule:
    - cron: '57 6 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/fullsdk-darwin/platform-tools platform/prebuilts/fullsdk-darwin/platform-tools

    - name: Push
      run: |
        cd platform/prebuilts/fullsdk-darwin/platform-tools
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/fullsdk-darwin/platform-tools.git