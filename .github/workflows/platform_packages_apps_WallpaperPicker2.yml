name: platform/packages/apps/WallpaperPicker2 Clone and Push

on:
  schedule:
    - cron: '4 18 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/WallpaperPicker2 platform/packages/apps/WallpaperPicker2

    - name: Push
      run: |
        cd platform/packages/apps/WallpaperPicker2
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/WallpaperPicker2.git