name: platform/frameworks/uiautomator Clone and Push

on:
  schedule:
    - cron: '24 13 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/uiautomator platform/frameworks/uiautomator

    - name: Push
      run: |
        cd platform/frameworks/uiautomator
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/uiautomator.git