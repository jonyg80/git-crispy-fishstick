name: platform/external/jackson-annotations Clone and Push

on:
  schedule:
    - cron: '27 9 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/jackson-annotations platform/external/jackson-annotations

    - name: Push
      run: |
        cd platform/external/jackson-annotations
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/jackson-annotations.git