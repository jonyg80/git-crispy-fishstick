name: platform/external/rust/crates/vmm-sys-util Clone and Push

on:
  schedule:
    - cron: '57 20 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/vmm-sys-util platform/external/rust/crates/vmm-sys-util

    - name: Push
      run: |
        cd platform/external/rust/crates/vmm-sys-util
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/rust/crates/vmm-sys-util.git