name: platform/tools/tradefederation Clone and Push

on:
  schedule:
    - cron: '10 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/tradefederation platform/tools/tradefederation

    - name: Push
      run: |
        cd platform/tools/tradefederation
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/tools/tradefederation.git