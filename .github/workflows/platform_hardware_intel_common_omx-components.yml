name: platform/hardware/intel/common/omx-components Clone and Push

on:
  schedule:
    - cron: '29 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/intel/common/omx-components platform/hardware/intel/common/omx-components

    - name: Push
      run: |
        cd platform/hardware/intel/common/omx-components
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/intel/common/omx-components.git