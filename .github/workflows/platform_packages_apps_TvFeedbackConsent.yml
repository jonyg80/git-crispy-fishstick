name: platform/packages/apps/TvFeedbackConsent Clone and Push

on:
  schedule:
    - cron: '30 21 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/TvFeedbackConsent platform/packages/apps/TvFeedbackConsent

    - name: Push
      run: |
        cd platform/packages/apps/TvFeedbackConsent
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/TvFeedbackConsent.git