name: platform/prebuilts/module_sdk/Media Clone and Push

on:
  schedule:
    - cron: '48 13 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/module_sdk/Media platform/prebuilts/module_sdk/Media

    - name: Push
      run: |
        cd platform/prebuilts/module_sdk/Media
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/module_sdk/Media.git