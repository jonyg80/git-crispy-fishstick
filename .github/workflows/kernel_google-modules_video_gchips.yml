name: kernel/google-modules/video/gchips Clone and Push

on:
  schedule:
    - cron: '51 10 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/kernel/google-modules/video/gchips kernel/google-modules/video/gchips

    - name: Push
      run: |
        cd kernel/google-modules/video/gchips
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/kernel/google-modules/video/gchips.git