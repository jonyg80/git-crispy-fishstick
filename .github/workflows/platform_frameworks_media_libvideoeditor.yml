name: platform/frameworks/media/libvideoeditor Clone and Push

on:
  schedule:
    - cron: '42 5 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/media/libvideoeditor platform/frameworks/media/libvideoeditor

    - name: Push
      run: |
        cd platform/frameworks/media/libvideoeditor
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/frameworks/media/libvideoeditor.git