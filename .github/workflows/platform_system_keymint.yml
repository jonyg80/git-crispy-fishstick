name: platform/system/keymint Clone and Push

on:
  schedule:
    - cron: '45 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/keymint platform/system/keymint

    - name: Push
      run: |
        cd platform/system/keymint
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/keymint.git