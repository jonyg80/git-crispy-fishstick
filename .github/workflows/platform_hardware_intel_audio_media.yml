name: platform/hardware/intel/audio_media Clone and Push

on:
  schedule:
    - cron: '51 10 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/intel/audio_media platform/hardware/intel/audio_media

    - name: Push
      run: |
        cd platform/hardware/intel/audio_media
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/intel/audio_media.git