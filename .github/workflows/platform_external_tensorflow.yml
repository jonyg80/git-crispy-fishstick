name: platform/external/tensorflow Clone and Push

on:
  schedule:
    - cron: '51 0 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/tensorflow platform/external/tensorflow

    - name: Push
      run: |
        cd platform/external/tensorflow
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/tensorflow.git