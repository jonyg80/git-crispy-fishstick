name: platform/external/gradle-perf-android-medium Clone and Push

on:
  schedule:
    - cron: '39 7 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/gradle-perf-android-medium platform/external/gradle-perf-android-medium

    - name: Push
      run: |
        cd platform/external/gradle-perf-android-medium
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/gradle-perf-android-medium.git