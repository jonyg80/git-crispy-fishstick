name: platform/external/apache-http Clone and Push

on:
  schedule:
    - cron: '15 18 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/apache-http platform/external/apache-http

    - name: Push
      run: |
        cd platform/external/apache-http
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/apache-http.git