name: platform/frameworks/proto_logging Clone and Push

on:
  schedule:
    - cron: '19 13 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/proto_logging platform/frameworks/proto_logging

    - name: Push
      run: |
        cd platform/frameworks/proto_logging
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/proto_logging.git