name: platform/system/tools/bpt Clone and Push

on:
  schedule:
    - cron: '21 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/tools/bpt platform/system/tools/bpt

    - name: Push
      run: |
        cd platform/system/tools/bpt
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/tools/bpt.git