name: platform/prebuilts/python/darwin-x86 Clone and Push

on:
  schedule:
    - cron: '42 14 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/python/darwin-x86 platform/prebuilts/python/darwin-x86

    - name: Push
      run: |
        cd platform/prebuilts/python/darwin-x86
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/python/darwin-x86.git