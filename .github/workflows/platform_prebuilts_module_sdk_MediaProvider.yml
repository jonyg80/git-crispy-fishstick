name: platform/prebuilts/module_sdk/MediaProvider Clone and Push

on:
  schedule:
    - cron: '22 23 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/module_sdk/MediaProvider platform/prebuilts/module_sdk/MediaProvider

    - name: Push
      run: |
        cd platform/prebuilts/module_sdk/MediaProvider
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/module_sdk/MediaProvider.git