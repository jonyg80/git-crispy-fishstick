name: platform/packages/services/AlternativeNetworkAccess Clone and Push

on:
  schedule:
    - cron: '20 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/services/AlternativeNetworkAccess platform/packages/services/AlternativeNetworkAccess

    - name: Push
      run: |
        cd platform/packages/services/AlternativeNetworkAccess
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/services/AlternativeNetworkAccess.git