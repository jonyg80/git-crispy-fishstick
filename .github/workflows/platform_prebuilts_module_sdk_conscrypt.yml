name: platform/prebuilts/module_sdk/conscrypt Clone and Push

on:
  schedule:
    - cron: '33 23 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/module_sdk/conscrypt platform/prebuilts/module_sdk/conscrypt

    - name: Push
      run: |
        cd platform/prebuilts/module_sdk/conscrypt
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/module_sdk/conscrypt.git