name: kernel/google-modules/bluetooth/synaptics Clone and Push

on:
  schedule:
    - cron: '30 9 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/kernel/google-modules/bluetooth/synaptics kernel/google-modules/bluetooth/synaptics

    - name: Push
      run: |
        cd kernel/google-modules/bluetooth/synaptics
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/kernel/google-modules/bluetooth/synaptics.git