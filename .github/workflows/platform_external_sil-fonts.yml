name: platform/external/sil-fonts Clone and Push

on:
  schedule:
    - cron: '29 10 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/sil-fonts platform/external/sil-fonts

    - name: Push
      run: |
        cd platform/external/sil-fonts
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/sil-fonts.git