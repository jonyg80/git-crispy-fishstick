name: platform/external/rust/crates/cobs Clone and Push

on:
  schedule:
    - cron: '1 5 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/cobs platform/external/rust/crates/cobs

    - name: Push
      run: |
        cd platform/external/rust/crates/cobs
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/cobs.git