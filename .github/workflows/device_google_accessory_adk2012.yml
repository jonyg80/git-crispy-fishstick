name: device/google/accessory/adk2012 Clone and Push

on:
  schedule:
    - cron: '57 1 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/device/google/accessory/adk2012 device/google/accessory/adk2012

    - name: Push
      run: |
        cd device/google/accessory/adk2012
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/device/google/accessory/adk2012.git