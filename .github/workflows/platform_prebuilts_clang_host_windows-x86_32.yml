name: platform/prebuilts/clang/host/windows-x86_32 Clone and Push

on:
  schedule:
    - cron: '33 20 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/clang/host/windows-x86_32 platform/prebuilts/clang/host/windows-x86_32

    - name: Push
      run: |
        cd platform/prebuilts/clang/host/windows-x86_32
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/clang/host/windows-x86_32.git