name: platform/external/clang-tools-extra Clone and Push

on:
  schedule:
    - cron: '39 23 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/clang-tools-extra platform/external/clang-tools-extra

    - name: Push
      run: |
        cd platform/external/clang-tools-extra
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/clang-tools-extra.git