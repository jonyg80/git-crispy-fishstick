name: device/htc/flounder-kernel Clone and Push

on:
  schedule:
    - cron: '42 5 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/device/htc/flounder-kernel device/htc/flounder-kernel

    - name: Push
      run: |
        cd device/htc/flounder-kernel
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/device/htc/flounder-kernel.git