name: platform/prebuilts/devtools Clone and Push

on:
  schedule:
    - cron: '9 6 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/devtools platform/prebuilts/devtools

    - name: Push
      run: |
        cd platform/prebuilts/devtools
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/devtools.git