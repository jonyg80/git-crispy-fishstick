name: platform/frameworks/libs/native_bridge_support Clone and Push

on:
  schedule:
    - cron: '35 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/libs/native_bridge_support platform/frameworks/libs/native_bridge_support

    - name: Push
      run: |
        cd platform/frameworks/libs/native_bridge_support
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/libs/native_bridge_support.git