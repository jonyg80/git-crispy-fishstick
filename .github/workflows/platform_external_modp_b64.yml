name: platform/external/modp_b64 Clone and Push

on:
  schedule:
    - cron: '4 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/modp_b64 platform/external/modp_b64

    - name: Push
      run: |
        cd platform/external/modp_b64
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/modp_b64.git