name: platform/external/sg3_utils Clone and Push

on:
  schedule:
    - cron: '23 10 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/sg3_utils platform/external/sg3_utils

    - name: Push
      run: |
        cd platform/external/sg3_utils
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/sg3_utils.git