name: platform/packages/apps/EmergencyInfo Clone and Push

on:
  schedule:
    - cron: '39 18 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/EmergencyInfo platform/packages/apps/EmergencyInfo

    - name: Push
      run: |
        cd platform/packages/apps/EmergencyInfo
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/EmergencyInfo.git