name: platform/external/minijail Clone and Push

on:
  schedule:
    - cron: '53 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/minijail platform/external/minijail

    - name: Push
      run: |
        cd platform/external/minijail
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/minijail.git