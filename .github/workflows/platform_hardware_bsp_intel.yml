name: platform/hardware/bsp/intel Clone and Push

on:
  schedule:
    - cron: '38 13 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/bsp/intel platform/hardware/bsp/intel

    - name: Push
      run: |
        cd platform/hardware/bsp/intel
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/bsp/intel.git