name: platform/external/cibu-fonts Clone and Push

on:
  schedule:
    - cron: '33 23 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/cibu-fonts platform/external/cibu-fonts

    - name: Push
      run: |
        cd platform/external/cibu-fonts
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/cibu-fonts.git