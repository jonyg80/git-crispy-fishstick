name: platform/external/ping6 Clone and Push

on:
  schedule:
    - cron: '19 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/ping6 platform/external/ping6

    - name: Push
      run: |
        cd platform/external/ping6
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/ping6.git