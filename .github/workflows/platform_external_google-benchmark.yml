name: platform/external/google-benchmark Clone and Push

on:
  schedule:
    - cron: '0 6 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/google-benchmark platform/external/google-benchmark

    - name: Push
      run: |
        cd platform/external/google-benchmark
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/google-benchmark.git