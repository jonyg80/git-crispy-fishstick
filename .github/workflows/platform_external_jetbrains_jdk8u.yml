name: platform/external/jetbrains/jdk8u Clone and Push

on:
  schedule:
    - cron: '24 10 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/jetbrains/jdk8u platform/external/jetbrains/jdk8u

    - name: Push
      run: |
        cd platform/external/jetbrains/jdk8u
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/jetbrains/jdk8u.git