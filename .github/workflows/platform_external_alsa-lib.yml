name: platform/external/alsa-lib Clone and Push

on:
  schedule:
    - cron: '6 17 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/alsa-lib platform/external/alsa-lib

    - name: Push
      run: |
        cd platform/external/alsa-lib
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/alsa-lib.git