name: platform/external/vulkan-tools Clone and Push

on:
  schedule:
    - cron: '46 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/vulkan-tools platform/external/vulkan-tools

    - name: Push
      run: |
        cd platform/external/vulkan-tools
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/vulkan-tools.git