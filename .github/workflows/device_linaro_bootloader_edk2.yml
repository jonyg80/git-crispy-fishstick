name: device/linaro/bootloader/edk2 Clone and Push

on:
  schedule:
    - cron: '33 6 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/device/linaro/bootloader/edk2 device/linaro/bootloader/edk2

    - name: Push
      run: |
        cd device/linaro/bootloader/edk2
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/device/linaro/bootloader/edk2.git