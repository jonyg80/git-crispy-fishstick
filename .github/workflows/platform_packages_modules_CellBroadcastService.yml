name: platform/packages/modules/CellBroadcastService Clone and Push

on:
  schedule:
    - cron: '19 18 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/modules/CellBroadcastService platform/packages/modules/CellBroadcastService

    - name: Push
      run: |
        cd platform/packages/modules/CellBroadcastService
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/modules/CellBroadcastService.git