name: platform/external/libtextclassifier Clone and Push

on:
  schedule:
    - cron: '0 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/libtextclassifier platform/external/libtextclassifier

    - name: Push
      run: |
        cd platform/external/libtextclassifier
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/libtextclassifier.git