name: platform/prebuilts/module_sdk/DeviceLock Clone and Push

on:
  schedule:
    - cron: '18 23 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/module_sdk/DeviceLock platform/prebuilts/module_sdk/DeviceLock

    - name: Push
      run: |
        cd platform/prebuilts/module_sdk/DeviceLock
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/module_sdk/DeviceLock.git