name: platform/prebuilts/abi-dumps/vndk Clone and Push

on:
  schedule:
    - cron: '46 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/abi-dumps/vndk platform/prebuilts/abi-dumps/vndk

    - name: Push
      run: |
        cd platform/prebuilts/abi-dumps/vndk
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/abi-dumps/vndk.git