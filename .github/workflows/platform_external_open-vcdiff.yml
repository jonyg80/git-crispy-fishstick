name: platform/external/open-vcdiff Clone and Push

on:
  schedule:
    - cron: '54 20 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/open-vcdiff platform/external/open-vcdiff

    - name: Push
      run: |
        cd platform/external/open-vcdiff
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/open-vcdiff.git