name: platform/tools/google_prebuilts/studio/sdk/remote Clone and Push

on:
  schedule:
    - cron: '36 23 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/google_prebuilts/studio/sdk/remote platform/tools/google_prebuilts/studio/sdk/remote

    - name: Push
      run: |
        cd platform/tools/google_prebuilts/studio/sdk/remote
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/tools/google_prebuilts/studio/sdk/remote.git