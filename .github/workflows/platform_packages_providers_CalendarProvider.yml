name: platform/packages/providers/CalendarProvider Clone and Push

on:
  schedule:
    - cron: '2 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/providers/CalendarProvider platform/packages/providers/CalendarProvider

    - name: Push
      run: |
        cd platform/packages/providers/CalendarProvider
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/providers/CalendarProvider.git