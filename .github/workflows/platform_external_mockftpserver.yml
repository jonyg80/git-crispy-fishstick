name: platform/external/mockftpserver Clone and Push

on:
  schedule:
    - cron: '0 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/mockftpserver platform/external/mockftpserver

    - name: Push
      run: |
        cd platform/external/mockftpserver
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/mockftpserver.git