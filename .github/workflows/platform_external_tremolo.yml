name: platform/external/tremolo Clone and Push

on:
  schedule:
    - cron: '23 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/tremolo platform/external/tremolo

    - name: Push
      run: |
        cd platform/external/tremolo
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/tremolo.git