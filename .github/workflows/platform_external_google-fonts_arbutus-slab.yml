name: platform/external/google-fonts/arbutus-slab Clone and Push

on:
  schedule:
    - cron: '12 6 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/google-fonts/arbutus-slab platform/external/google-fonts/arbutus-slab

    - name: Push
      run: |
        cd platform/external/google-fonts/arbutus-slab
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/google-fonts/arbutus-slab.git