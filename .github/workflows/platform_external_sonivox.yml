name: platform/external/sonivox Clone and Push

on:
  schedule:
    - cron: '39 10 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/sonivox platform/external/sonivox

    - name: Push
      run: |
        cd platform/external/sonivox
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/sonivox.git