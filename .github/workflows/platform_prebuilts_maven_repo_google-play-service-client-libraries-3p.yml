name: platform/prebuilts/maven_repo/google-play-service-client-libraries-3p Clone and Push

on:
  schedule:
    - cron: '15 13 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/maven_repo/google-play-service-client-libraries-3p platform/prebuilts/maven_repo/google-play-service-client-libraries-3p

    - name: Push
      run: |
        cd platform/prebuilts/maven_repo/google-play-service-client-libraries-3p
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/maven_repo/google-play-service-client-libraries-3p.git