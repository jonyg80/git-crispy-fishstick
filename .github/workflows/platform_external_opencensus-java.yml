name: platform/external/opencensus-java Clone and Push

on:
  schedule:
    - cron: '45 1 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/opencensus-java platform/external/opencensus-java

    - name: Push
      run: |
        cd platform/external/opencensus-java
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/opencensus-java.git