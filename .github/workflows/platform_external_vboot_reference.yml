name: platform/external/vboot_reference Clone and Push

on:
  schedule:
    - cron: '39 2 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/vboot_reference platform/external/vboot_reference

    - name: Push
      run: |
        cd platform/external/vboot_reference
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/vboot_reference.git