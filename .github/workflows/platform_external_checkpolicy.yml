name: platform/external/checkpolicy Clone and Push

on:
  schedule:
    - cron: '18 21 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/checkpolicy platform/external/checkpolicy

    - name: Push
      run: |
        cd platform/external/checkpolicy
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/checkpolicy.git