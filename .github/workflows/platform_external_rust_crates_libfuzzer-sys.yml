name: platform/external/rust/crates/libfuzzer-sys Clone and Push

on:
  schedule:
    - cron: '35 6 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/libfuzzer-sys platform/external/rust/crates/libfuzzer-sys

    - name: Push
      run: |
        cd platform/external/rust/crates/libfuzzer-sys
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/rust/crates/libfuzzer-sys.git