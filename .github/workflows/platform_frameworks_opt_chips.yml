name: platform/frameworks/opt/chips Clone and Push

on:
  schedule:
    - cron: '51 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/opt/chips platform/frameworks/opt/chips

    - name: Push
      run: |
        cd platform/frameworks/opt/chips
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/opt/chips.git