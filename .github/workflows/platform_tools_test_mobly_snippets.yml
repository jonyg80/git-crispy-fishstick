name: platform/tools/test/mobly_snippets Clone and Push

on:
  schedule:
    - cron: '6 1 * * 5' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/test/mobly_snippets platform/tools/test/mobly_snippets

    - name: Push
      run: |
        cd platform/tools/test/mobly_snippets
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/tools/test/mobly_snippets.git