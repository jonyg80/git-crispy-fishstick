name: platform/system/firewalld Clone and Push

on:
  schedule:
    - cron: '33 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/system/firewalld platform/system/firewalld

    - name: Push
      run: |
        cd platform/system/firewalld
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/system/firewalld.git