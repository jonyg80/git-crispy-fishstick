name: platform/external/actionbarsherlock Clone and Push

on:
  schedule:
    - cron: '51 16 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/actionbarsherlock platform/external/actionbarsherlock

    - name: Push
      run: |
        cd platform/external/actionbarsherlock
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/actionbarsherlock.git