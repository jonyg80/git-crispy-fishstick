name: platform/external/libnfc-nxp Clone and Push

on:
  schedule:
    - cron: '51 14 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/libnfc-nxp platform/external/libnfc-nxp

    - name: Push
      run: |
        cd platform/external/libnfc-nxp
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/libnfc-nxp.git