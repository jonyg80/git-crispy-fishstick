name: platform/frameworks/libs/service_entitlement Clone and Push

on:
  schedule:
    - cron: '37 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/libs/service_entitlement platform/frameworks/libs/service_entitlement

    - name: Push
      run: |
        cd platform/frameworks/libs/service_entitlement
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/libs/service_entitlement.git