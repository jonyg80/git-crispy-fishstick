name: device/linaro/dragonboard Clone and Push

on:
  schedule:
    - cron: '36 6 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/device/linaro/dragonboard device/linaro/dragonboard

    - name: Push
      run: |
        cd device/linaro/dragonboard
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/device/linaro/dragonboard.git