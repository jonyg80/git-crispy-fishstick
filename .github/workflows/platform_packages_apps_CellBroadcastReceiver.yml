name: platform/packages/apps/CellBroadcastReceiver Clone and Push

on:
  schedule:
    - cron: '49 16 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/CellBroadcastReceiver platform/packages/apps/CellBroadcastReceiver

    - name: Push
      run: |
        cd platform/packages/apps/CellBroadcastReceiver
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/CellBroadcastReceiver.git