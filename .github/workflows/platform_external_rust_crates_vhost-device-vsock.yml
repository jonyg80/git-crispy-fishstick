name: platform/external/rust/crates/vhost-device-vsock Clone and Push

on:
  schedule:
    - cron: '36 20 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/vhost-device-vsock platform/external/rust/crates/vhost-device-vsock

    - name: Push
      run: |
        cd platform/external/rust/crates/vhost-device-vsock
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/rust/crates/vhost-device-vsock.git