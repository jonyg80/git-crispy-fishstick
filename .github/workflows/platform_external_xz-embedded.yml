name: platform/external/xz-embedded Clone and Push

on:
  schedule:
    - cron: '15 4 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/xz-embedded platform/external/xz-embedded

    - name: Push
      run: |
        cd platform/external/xz-embedded
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/xz-embedded.git