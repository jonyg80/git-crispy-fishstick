name: platform/frameworks/compile/slang Clone and Push

on:
  schedule:
    - cron: '25 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/compile/slang platform/frameworks/compile/slang

    - name: Push
      run: |
        cd platform/frameworks/compile/slang
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/compile/slang.git