name: platform/external/perf_data_converter Clone and Push

on:
  schedule:
    - cron: '12 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/perf_data_converter platform/external/perf_data_converter

    - name: Push
      run: |
        cd platform/external/perf_data_converter
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/perf_data_converter.git