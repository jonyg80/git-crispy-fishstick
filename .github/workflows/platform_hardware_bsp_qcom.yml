name: platform/hardware/bsp/qcom Clone and Push

on:
  schedule:
    - cron: '2 14 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/bsp/qcom platform/hardware/bsp/qcom

    - name: Push
      run: |
        cd platform/hardware/bsp/qcom
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/bsp/qcom.git