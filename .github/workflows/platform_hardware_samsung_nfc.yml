name: platform/hardware/samsung/nfc Clone and Push

on:
  schedule:
    - cron: '42 15 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/samsung/nfc platform/hardware/samsung/nfc

    - name: Push
      run: |
        cd platform/hardware/samsung/nfc
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/hardware/samsung/nfc.git