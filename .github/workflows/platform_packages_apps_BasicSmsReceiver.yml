name: platform/packages/apps/BasicSmsReceiver Clone and Push

on:
  schedule:
    - cron: '0 16 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/BasicSmsReceiver platform/packages/apps/BasicSmsReceiver

    - name: Push
      run: |
        cd platform/packages/apps/BasicSmsReceiver
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/BasicSmsReceiver.git