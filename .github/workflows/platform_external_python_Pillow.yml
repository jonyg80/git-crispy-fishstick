name: platform/external/python/Pillow Clone and Push

on:
  schedule:
    - cron: '27 23 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/python/Pillow platform/external/python/Pillow

    - name: Push
      run: |
        cd platform/external/python/Pillow
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/python/Pillow.git