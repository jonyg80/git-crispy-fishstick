name: platform/external/webrtc Clone and Push

on:
  schedule:
    - cron: '54 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/webrtc platform/external/webrtc

    - name: Push
      run: |
        cd platform/external/webrtc
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/webrtc.git