name: platform/prebuilts/gcc/linux-x86/mips/mipsel-linux-android-4.8 Clone and Push

on:
  schedule:
    - cron: '33 22 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/mips/mipsel-linux-android-4.8 platform/prebuilts/gcc/linux-x86/mips/mipsel-linux-android-4.8

    - name: Push
      run: |
        cd platform/prebuilts/gcc/linux-x86/mips/mipsel-linux-android-4.8
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/gcc/linux-x86/mips/mipsel-linux-android-4.8.git