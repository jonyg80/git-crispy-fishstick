name: platform/hardware/samsung_slsi/exynos5 Clone and Push

on:
  schedule:
    - cron: '54 14 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/hardware/samsung_slsi/exynos5 platform/hardware/samsung_slsi/exynos5

    - name: Push
      run: |
        cd platform/hardware/samsung_slsi/exynos5
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/hardware/samsung_slsi/exynos5.git