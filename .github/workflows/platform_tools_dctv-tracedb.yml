name: platform/tools/dctv-tracedb Clone and Push

on:
  schedule:
    - cron: '22 2 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/dctv-tracedb platform/tools/dctv-tracedb

    - name: Push
      run: |
        cd platform/tools/dctv-tracedb
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/tools/dctv-tracedb.git