name: platform/frameworks/opt/inputmethodcommon Clone and Push

on:
  schedule:
    - cron: '0 13 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/opt/inputmethodcommon platform/frameworks/opt/inputmethodcommon

    - name: Push
      run: |
        cd platform/frameworks/opt/inputmethodcommon
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/opt/inputmethodcommon.git