name: platform/external/vulkan-headers Clone and Push

on:
  schedule:
    - cron: '45 11 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/vulkan-headers platform/external/vulkan-headers

    - name: Push
      run: |
        cd platform/external/vulkan-headers
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/vulkan-headers.git