name: platform/external/linux-kselftest Clone and Push

on:
  schedule:
    - cron: '25 0 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/linux-kselftest platform/external/linux-kselftest

    - name: Push
      run: |
        cd platform/external/linux-kselftest
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/linux-kselftest.git