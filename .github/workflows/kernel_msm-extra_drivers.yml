name: kernel/msm-extra/drivers Clone and Push

on:
  schedule:
    - cron: '57 11 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/kernel/msm-extra/drivers kernel/msm-extra/drivers

    - name: Push
      run: |
        cd kernel/msm-extra/drivers
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/kernel/msm-extra/drivers.git