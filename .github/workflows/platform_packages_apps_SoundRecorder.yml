name: platform/packages/apps/SoundRecorder Clone and Push

on:
  schedule:
    - cron: '45 20 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/SoundRecorder platform/packages/apps/SoundRecorder

    - name: Push
      run: |
        cd platform/packages/apps/SoundRecorder
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/packages/apps/SoundRecorder.git