name: platform/frameworks/libs/net Clone and Push

on:
  schedule:
    - cron: '36 12 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/libs/net platform/frameworks/libs/net

    - name: Push
      run: |
        cd platform/frameworks/libs/net
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/frameworks/libs/net.git