name: platform/prebuilts/au-generator Clone and Push

on:
  schedule:
    - cron: '4 20 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/au-generator platform/prebuilts/au-generator

    - name: Push
      run: |
        cd platform/prebuilts/au-generator
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/prebuilts/au-generator.git