name: platform/external/nanopb-c Clone and Push

on:
  schedule:
    - cron: '24 19 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/nanopb-c platform/external/nanopb-c

    - name: Push
      run: |
        cd platform/external/nanopb-c
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/nanopb-c.git