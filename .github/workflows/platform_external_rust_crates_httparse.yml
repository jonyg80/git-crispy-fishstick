name: platform/external/rust/crates/httparse Clone and Push

on:
  schedule:
    - cron: '39 10 * * 2' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/rust/crates/httparse platform/external/rust/crates/httparse

    - name: Push
      run: |
        cd platform/external/rust/crates/httparse
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/rust/crates/httparse.git