name: platform/external/exfatprogs Clone and Push

on:
  schedule:
    - cron: '9 3 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/exfatprogs platform/external/exfatprogs

    - name: Push
      run: |
        cd platform/external/exfatprogs
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/exfatprogs.git