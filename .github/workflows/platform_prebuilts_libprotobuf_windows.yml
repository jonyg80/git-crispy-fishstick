name: platform/prebuilts/libprotobuf/windows Clone and Push

on:
  schedule:
    - cron: '57 12 * * 4' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/prebuilts/libprotobuf/windows platform/prebuilts/libprotobuf/windows

    - name: Push
      run: |
        cd platform/prebuilts/libprotobuf/windows
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/prebuilts/libprotobuf/windows.git