name: platform/frameworks/compile/llvm-ndk-cc Clone and Push

on:
  schedule:
    - cron: '54 4 * * 3' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/frameworks/compile/llvm-ndk-cc platform/frameworks/compile/llvm-ndk-cc

    - name: Push
      run: |
        cd platform/frameworks/compile/llvm-ndk-cc
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/frameworks/compile/llvm-ndk-cc.git