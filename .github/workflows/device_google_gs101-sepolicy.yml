name: device/google/gs101-sepolicy Clone and Push

on:
  schedule:
    - cron: '51 3 * * 0' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/device/google/gs101-sepolicy device/google/gs101-sepolicy

    - name: Push
      run: |
        cd device/google/gs101-sepolicy
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/device/google/gs101-sepolicy.git