name: toolchain/prebuilts/ndk/r18 Clone and Push

on:
  schedule:
    - cron: '3 5 * * 5' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/toolchain/prebuilts/ndk/r18 toolchain/prebuilts/ndk/r18

    - name: Push
      run: |
        cd toolchain/prebuilts/ndk/r18
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/toolchain/prebuilts/ndk/r18.git