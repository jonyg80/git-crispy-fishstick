name: platform/external/igt-gpu-tools Clone and Push

on:
  schedule:
    - cron: '51 8 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/igt-gpu-tools platform/external/igt-gpu-tools

    - name: Push
      run: |
        cd platform/external/igt-gpu-tools
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/igt-gpu-tools.git