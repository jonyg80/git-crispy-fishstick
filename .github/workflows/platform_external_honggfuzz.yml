name: platform/external/honggfuzz Clone and Push

on:
  schedule:
    - cron: '27 8 * * 1' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/honggfuzz platform/external/honggfuzz

    - name: Push
      run: |
        cd platform/external/honggfuzz
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/external/honggfuzz.git