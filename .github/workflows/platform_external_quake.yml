name: platform/external/quake Clone and Push

on:
  schedule:
    - cron: '53 3 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/external/quake platform/external/quake

    - name: Push
      run: |
        cd platform/external/quake
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/external/quake.git