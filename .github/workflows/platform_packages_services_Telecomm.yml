name: platform/packages/services/Telecomm Clone and Push

on:
  schedule:
    - cron: '30 19 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/services/Telecomm platform/packages/services/Telecomm

    - name: Push
      run: |
        cd platform/packages/services/Telecomm
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/services/Telecomm.git