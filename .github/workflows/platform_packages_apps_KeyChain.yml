name: platform/packages/apps/KeyChain Clone and Push

on:
  schedule:
    - cron: '12 17 * * *' # Runs every day

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/packages/apps/KeyChain platform/packages/apps/KeyChain

    - name: Push
      run: |
        cd platform/packages/apps/KeyChain
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitURL }}/platform/packages/apps/KeyChain.git