name: platform/tools/studio/google/services Clone and Push

on:
  schedule:
    - cron: '45 0 * * 5' # Runs every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Git clone
      run: git clone --mirror https://android.googlesource.com/platform/tools/studio/google/services platform/tools/studio/google/services

    - name: Push
      run: |
        cd platform/tools/studio/google/services
        git config --global user.email "you@example.com"
        git config --global user.name "Your Name"
        git push --mirror https://${{ secrets.GitUsername }}:${{ secrets.GitToken }}@${{ secrets.GitURL }}/platform/tools/studio/google/services.git